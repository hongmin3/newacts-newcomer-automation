# 2026-08-12 TEST 모드 실행 결과

사용자 승인 후 두 Apps Script 프로젝트에서 테스트 전용 함수를 실행했습니다. 실행 당시 두 설정은 `active: false`, `mode: 'TEST'`였으며 메일 수신자는 `ksj747172@gmail.com` 한 주소로 강제되었습니다.

## 실행 결과

| 테스트 | 결과 | 주요 수치 |
|---|---|---|
| `runEducationTest` | 성공 | 응답 27건, 신규 0건, 출석 반영 2건, 기존 기입/중복 25건, 정보 변경 0건, 수동 확인 0건 |
| `runNewcomerNotificationTest` | 성공 | 교육 진행 46명, 미진행 128명 |
| `runRegistrationMaintenanceTest` | 성공 | 군 현황 처리 416명, 출력 106행, 방문자 신규 추가 13명, 등록 표시 0건, 검토 63건 |
| `runRegistrationReportingTest` | 성공 | 등록 422명, 교육 참여 184명, 수료 126명, 진행 중 58명, 미시작 238명, 수료율 29.9% |

수료 보고에서는 군/팀 불일치 68건과 중복 전화번호 1건을 자동 덮어쓰지 않고 검토 대상으로 분리했습니다. 유지보수 테스트 후 다시 실행한 미리보기에서는 방문자 신규 추가가 0명으로 확인되어, 최초 테스트에서 추가한 13명이 중복으로 재추가되지 않았습니다.

## 메일 안전성 확인

Gmail에서 같은 날의 `[TEST]` 제목 메일을 검색해 다음 네 종류가 본인 받은편지함에 도착한 것을 확인했습니다.

- `[TEST] [새가족교육 자동화] 승인된 테스트 실행 결과`
- `[TEST] [뉴액츠 새가족부] 금주 새가족 교육 문자공지 명단 (2026-08-12)`
- `[TEST] [새가족 자동화] 승인된 테스트 결과`
- `[TEST] [새가족부] 등록자 새가족교육 수료 통계 - 승인된 테스트`

TEST 모드에서는 군별 담당자 메일 루프 자체가 실행되지 않으며, 공통 메일 관문도 본인 한 명 이외의 수신자를 거부합니다.

## 생성·갱신된 로그

- 교육 프로젝트: `자동화 로그`
- 등록 프로젝트: `자동화 로그`, `수료 자동화 로그`

## 아직 수행하지 않은 작업

- 집중교육 반영 테스트
- 교육 신규 응답 커서 초기화
- `active: true`, `mode: 'PRODUCTION'` 운영 전환
- 개선본 Git 커밋 및 푸시

위 작업은 사용자 승인 후에만 수행합니다.

## 군별 상세 메일 추가 테스트

사용자 요청으로 군별 메일에 상세 명단 표를 추가하고 TEST 모드에서 다시 실행했습니다.

- 대상 군: 신·조·총·석·전·명·임·슬·영, 총 9개 군
- 수신자: 모든 군 메일을 `ksj747172@gmail.com`으로 강제
- 구성: 상단 요약 카드와 `수료 / 진행 중 / 미시작` 명단 표
- 명단 열: 이름, 팀, 연락처, 1주, 2주, 3주, 4주
- 표시: 출석은 `●`, 미출석은 `–`
- 결과: 전체 요약 1통과 군별 상세 9통이 오류 없이 도착

운영 모드에서는 동일한 군별 상세 형식을 사용하되 각 메일은 원본 코드에 보존된 해당 군 담당자 주소로 발송됩니다.

### 사용자 피드백 반영 대기 중인 최종 형식

- 상단에 `1회 이상 교육 참여율 = 교육 참여 인원 ÷ 등록 인원` 추가
- 수료 명단: 이름, 팀, 연락처, 수료일
- 진행 중 명단: 이름, 팀, 연락처, 마지막 교육일
- 미시작 명단: 이름, 팀, 연락처, 등록일
- 기존 1~4주차 기호 열은 제거하여 상태별 핵심 날짜가 더 잘 보이도록 단순화

이 형식은 코드 및 Apps Script에 저장하되, 추가 메일 발송 테스트는 사용자 재승인 후 실행합니다.

### 추가 표시 개선

- 전체 교육 현황도 군별 메일과 동일하게 상태별 상세 명단 표를 포함
- 모든 요약·명단·불일치 표의 제목, 헤더, 셀을 가운데 정렬
- 군/팀 불일치는 `등록 시트의 현재 값`과 `교육 시트 입력 값`을 나란히 표시
- 어느 값이 맞는지 자동으로 판단하지 않았으며 등록 값을 유지했다는 설명과 수동 확인 방법을 명시

### 최종 형식 테스트 결과

- 전체 요약 1통과 군별 상세 9통이 TEST 모드로 본인에게 도착
- 군별 메일에서 1회 이상 참여율과 수료일·마지막 교육일·등록일 표시 확인
- 전체 메일에 422명의 상세 명단을 모두 넣었을 때 Gmail의 메일 내용 잘림이 발생함
- 전체 메일은 잘림을 방지하기 위해 9개 군 비교 요약 표로 변경
- 개인별 상세 명단은 해당 군 메일에 유지
- 사용자 승인 후 최종 형식으로 전체 1통과 군별 9통을 재발송해 모두 도착 확인
- 전체 메일: Gmail 내용 잘림 없음, 9개 군 비교표·참여율·불일치 설명 및 비교표 확인
- 군별 메일: Gmail 내용 잘림 없음, 수료일·마지막 교육일·등록일 및 참여율 확인

## 운영 승인

2026-08-14 사용자가 최종 메일 형식과 운영 수신자 복원을 승인했습니다. 전체 메일 제목은 `[새가족부] 전체 새가족교육 현황 - …`으로 명확히 변경했으며, 교육 응답 기준점은 450행으로 초기화한 뒤 운영 모드로 전환했습니다.
