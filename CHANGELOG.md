# Changelog

이 파일은 **무엇이 바뀌었는가**만 담는다. 현재 사양은 `SPEC.md`, 현재 작업 진행 상태는
`progress.md`에 있다. 같은 내용을 두 곳에 쓰지 않는다.

가능하면 각 항목 앞에 Requirement ID를 붙인다.

```text
### Changed
- REQ-EXPORT-002: HTML 파일명에 실행 시간을 포함하도록 변경

### Fixed
- REQ-QUERY-001: 인증 만료 시 잘못된 성공 상태를 반환하던 문제 수정
```

Semantic Versioning은 강제하지 않는다. 프로젝트에 Versioning 정책이 있으면 그것을 따른다.

## [Unreleased]

### Added

- NFR-DATA-001 / NFR-SYNC-001: 보존·동기화 계약 검사
  (`tests/data-retention-and-sync.test.js`, TEST-DATA-001 · TEST-SYNC-001). 여섯 개
  `.gs`에 자동 삭제·만료 코드가 없는지, 매니페스트의 네 프로젝트와 clasp 인증 파일
  비추적, push 후 재확인 경로가 남아 있는지 본다.
- REQ-EDU-003 · REQ-NOTI-001 · NFR-OPS-001: 이미 있던 자동 테스트를 추적성 표에
  연결하는 TEST 절을 추가했다(TEST-EDU-003 · TEST-NOTI-001 · TEST-OPS-001).
  `tests/education-automation.test.js`는 본문 주석에 `TEST-EDU-003`을 적고 있었는데
  SPEC에는 그 ID가 없었다.
- REQ-EDU-001 · REQ-EDU-002 · REQ-REPORT-001: 운영 시트가 있어야 관찰되는 요구사항의
  수동 절차를 추가했다. 시트를 바꾸지 않는 미리보기 함수만 쓴다.

### Changed

- REQ-REG-002: 현황판에서 `스스로`로 보는 세 조건을 코드와 테스트에 맞춰 모두 적었다. 군 값이 `스스로` 또는 `군배정필요`인 경우와, 군이 비고 소개자가 `스스로`인 경우다.
- SPEC 문장을 처음 보는 사람도 읽을 수 있게 다시 썼다. 긴 문단은 나누고, 차례는 번호 목록, 예외는 `> **예외**` 상자, 이유는 `이유:` 줄로 옮겼다. 1절에 용어 표(`이 문서에서 쓰는 말`)를 더했다. 사양 내용(규칙·숫자·날짜·코드 이름·요구사항 ID·추적성 표)은 바꾸지 않았다(공통 SPEC workflow v5·렌더러 v5).
- 문서: 공통 SPEC workflow v4(AGENTS.md) — SPEC·CHANGELOG 작성 규칙(제목 이름, 기능 그룹 표, CHANGELOG ID, 예시 블록 금지, flow 흐름도, 로컬 이미지, HTML 재생성)을 한 절로 모았다. `docs/SPEC.html` 렌더러 v4: 왼쪽 목차에 지금 읽는 절·요구사항 표시.
- 준비 검사: CHANGELOG 형식 예시 블록에 실제 항목이 들어가면 `CHANGELOG_EXAMPLE_MODIFIED`로 경고한다(키트 관리 사본 `.project-check/project-readiness.js`, 기준 `.project-check/changelog-template.md`).
- 문서: `docs/SPEC.html`을 렌더러 v3로 다시 만들었다 — 이력이 없는 요구사항에 "기록된 변경 없음" 표시.
- 문서: `SPEC.md`의 REQ·NFR 제목 줄 20개에 기능 이름을 붙이고 5절에 기능 그룹 표를 추가했다.
  사양 내용은 바꾸지 않았다. 사람이 읽는 `docs/SPEC.html`(기능 목록·요구사항 카드·요구사항별
  변경 이력)과 렌더러 `.project-check/render-spec-html.js`를 추가하고 공통 SPEC workflow를 v3로
  갱신했다 — `SPEC.md`나 `CHANGELOG.md`를 고치면 HTML을 다시 만든다.
- 추적성 표의 한글 `.gs` 경로가 backtick 없이 적혀 있어 검사기가 구현 참조로 인정하지
  못하던 것을 14행 고쳤다. 경로 자체는 처음부터 맞았다.

- 시트 메뉴에서 `변경 예정 미리보기`·`승인된 테스트 실행`을 제거(운영에서 쓰지 않음).
  두 함수는 Apps Script 편집기에서 쓸 수 있도록 그대로 둔다
- REQ-REG-002: 군 현황판 배정에서 4부·5부 구분을 제거 — 같은 군이면 예배 구분과 무관하게
  한 열에 모인다. `스스로`만 4부·5부 열로 나눠 표시한다. 현황판 열 구성이 20열에서
  `날짜 / 군 9개 / 스스로 4부·5부 / 미배정 / 합계` 14열로 바뀌고 1~2행 머리글도 자동화가 쓴다
- REQ-REG-002: 현황판에서 누락되는 등록자를 없앴다 — 군을 확정하지 못한 사람과 예배 구분 없는
  스스로 등록자는 빠지는 대신 `미배정` 열에 배치되고 검토 내역으로도 보고된다.
  군 표기는 첫 글자로 정규화해 `신군`·`신1팀`을 `신`으로 살린다
- REQ-REG-001: 등록정보 자동 보정에서 이름을 제외 — 이름이 교육 출석과 다르면 고치지 않고
  검토 내역으로 보고
- REQ-REG-001: 정기 실행 메일 수신 범위를 테스트 수신자 한 명으로 유지하기로 확정
- REQ-EDU-003: `runIntensiveTrainingTest` → `applyIntensiveTrainingNow` (실제 시트 변경을
  이름과 안내에 명시)
- REQ-ATTEND-002: 출석 응답 2행 최신순 삽입 유지 — 과거 보강 문서를 현재 구현에 맞게 수정
- NFR-OPS-001: 교육 출석 반영 트리거를 화요일 17:00~18:00 한 번으로 확정(금요일 제거).
  `removeFridayEducationTrigger` 반영·확인 완료. API 원격 실행이 막혀 있어 금요일 트리거
  실제 삭제는 편집기에서 함수를 1회 실행하는 수동 1단계로 남김(현재 트리거 문서 참조)
- NFR-DATA-001: 출석 원본·로그·보정 상태를 자동 삭제하지 않기로 확정
- SPEC.md: Project Version 1.0.0, Owner 역할명 지정

### Fixed

- REQ-REG-002: 필터가 걸린 시트에서 머리글 세로 병합이 거부되며(`필터 헤더 위에는 수직
  병합을 만들 수 없습니다`) 명단 기록 전에 실행이 중단되던 문제 수정. 세로 병합을 없애고,
  가로 병합은 실패해도 건너뛴다. 명단을 먼저 쓰고 머리글을 나중에 쓰도록 순서를 바꿔
  머리글 실패가 명단을 막지 못하게 했다
- REQ-REG-002: 세로 병합을 포기하면서 `날짜`·`미배정`·`합계`를 1·2행에 모두 적어 머리글이
  두 줄로 겹쳐 보이던 문제 수정. 1행은 묶음 이름 전용, 2행이 열 이름 행으로 역할을 나눴다
- 테스트의 가짜 시트가 `merge()`를 항상 성공시키던 문제 수정. 실제 API가 거부하는 조건을
  모델링하도록 바꿨고(TEST-REG-005), 이 테스트는 수정 전 구현에서 실제로 실패한다

### Added

- REQ-REG-004: 현황판 3행에 합계 행 추가 — 군별·스스로·미배정 열의 인원 수와 전체 인원.
  명단은 4행부터. 합계는 집계 변수를 복사하지 않고 출력된 칸을 다시 세어 만든다
- REQ-REG-004: 인원 대조 알고리즘 — 등록 명단 인원 / 배치 집계 / 출력 칸 수 / 주별 합계 합,
  네 경로의 수를 맞춰 본다. 불일치면 빠진 사람의 이름과 등록 행 번호를 결과 문장에 넣고
  검토 내역과 메일 제목에 표시한다. 일치할 때도 근거 인원 수를 함께 적는다
- REQ-REG-003: 스스로 등록자와 군 미배정자를 하나의 명단으로 모아 등록 유지보수 결과 메일의
  독립 섹션(텍스트·HTML)으로 발송. 메일 제목에도 인원 수 노출
- SPEC 8.1: 회기 전환 시 군 구성 변경 절차 — 네 Apps Script 프로젝트에 걸친 7개 수정 위치와
  확인 명령, 시트 복사 시 스프레드시트 ID 교체 지침
- `REGISTRATION_AUTOMATION.groups`: 등록·보고 프로젝트의 군 목록 단일 출처. 현황판 열 순서와
  열 수(`군 수 + 5`), 군 표기 정규화가 모두 여기서 파생된다
- REQ-SETTLE-001: 상반기 결산 집계·목표 대비 비율 계산을 정식 요구사항과 테스트로 추가
- TEST-REG-002, TEST-REG-003, TEST-REG-004, TEST-REG-005, TEST-REG-006, TEST-SETTLE-001

## [1.0.0] - 2026-09-19

### Added

- SPEC.md 도입 — 기존 동작을 Requirement로 문서화 (REQ-*/NFR-*)
