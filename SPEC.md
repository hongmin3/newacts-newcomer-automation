# 뉴액츠 새가족·교육 통합 자동화 사양서

<!-- spec-template: v1 -->

| 항목 | 값 |
|---|---|
| Document Version | 1.0.0 |
| Project Version | 1.0.0 |
| Last Updated | 2026-09-19 |
| Status | active |
| Owner | 새가족부 운영 담당자 (역할명) |

이 문서는 뉴액츠 새가족 자동화가 **어떻게 동작해야 하는가**를 정의하는 기준이다.
코드가 현재 그렇게 동작한다는 사실은 사양이 아니다. 사양과 구현이 다르면 코드에 맞춰 이
문서를 고치지 말고 `SPEC / CODE MISMATCH`로 식별한다 — 절차는 `AGENTS.md`에 있다.

## 1. 목적

새가족부 운영진이 새가족의 **등록 → 교육 출석 → 수료 → 보고**를 손으로 옮겨 적지 않고
운영하기 위한 자동화다. 출석 응답이 들어오면 사람이 개입하지 않아도 교육 명단·등록 명단·
수료현황이 서로 맞고, 주간 운영 판단에 필요한 명단과 통계가 정해진 시각에 담당자에게
메일로 도착하는 것이 성공 조건이다.

자동으로 판단할 수 없는 건은 **임의로 고치지 않고 검토 대상으로 보고**한다. 조용히 틀린
값을 쓰는 것보다 사람이 확인하도록 남기는 쪽이 항상 옳다.

## 2. 프로젝트 범위

### 포함

- 새가족 교육 출석 입력 웹앱(본인 확인, 주차 검증, 응답 시트 기록)
- Google Form 출석 응답을 교육 출석 현황 시트에 증분 반영
- 집중교육 참석 결과를 교육 출석 현황에 병합
- 등록 명단의 군·팀·전화번호를 교육 출석 기준으로 자동 보정하고 군 현황판 갱신
- 교육 출석과 등록 명단을 결합한 수료현황 재작성 및 주간 전체·군별 보고 메일
- 토요일 문자공지 대상 명단(교육 진행 중 / 교육 미진행) 메일
- 집중교육 신청 Form 접수와 관리자·공개 명단 실시간 동기화
- 실행 결과의 숨김 로그 기록과 메일 수신자 안전장치
- 저장소 ↔ 운영 Apps Script 동기화 도구(`scripts/apps-script.mjs`)

### 제외

- 실제 문자(SMS) 발송. 이 자동화는 **발송 대상 명단과 번호 목록까지만** 만든다.
- 정착률 계산과 월간 정착률 메일. 외부 시스템 로그인이 필요해 Apps Script 실행 기반
  밖에 있다(`docs/settlement-monthly-email-analysis.md`).
- 개인 명단·실행 결과 데이터의 저장소 보관. 데이터는 Google Sheets에만 둔다.
- 교육·등록 정기 트리거의 자동 생성·변경. 이 일정은 Apps Script 화면에서 사람이 관리한다.
  집중교육 신청 설치 함수의 제출 트리거 생성은 포함한다.
- 운영 배포의 자동화. 배포는 명시적 승인 후 수동으로 실행한다.

## 3. 시스템 구성

네 개의 독립된 Apps Script 프로젝트로 이루어진다. 한 디렉터리 = 한 Apps Script 프로젝트이며,
같은 디렉터리의 `.gs` 파일은 전역 설정과 공통 함수를 공유하므로 독립 스크립트로 분리하지
않는다.

| 구성 요소 | 책임 | 위치 |
|---|---|---|
| 출석 웹앱 | 교육 참석자 본인 확인, 주차 검증, 응답 시트 기록 | `attendance-webapp/` |
| 교육 관리 | 응답 증분 반영, 집중교육 병합, 문자공지 명단, 교육 메일 관문 | `education-project/` |
| 등록·보고 | 등록정보 자동 보정, 군 현황판, 수료현황, 주간 보고 메일, 결산 | `registration-project/` |
| 집중교육 신청 | Form·관리자/공개 Spreadsheet 설치와 제출 시 실시간 동기화 | `intensive-training-application/` |
| 동기화 도구 | 저장소 ↔ 운영 `diff`/`pull`/`push`/`deploy` | `scripts/apps-script.mjs` |
| 정적 검증 | Apps Script API를 호출하지 않는 로컬 단위·소스 검증 | `tests/` |

외부 시스템 경계는 Google Sheets, Google Forms, Google Drive, `MailApp`,
`PropertiesService`, `LockService`이며 모두 Apps Script 실행 계정 권한으로 접근한다.
저장소 쪽 경계는 Apps Script API(`scripts/apps-script.mjs`)와 clasp가 저장한 refresh token
하나다.

## 4. 전체 동작 흐름

1. 참석자가 출석 웹앱에서 이름·전화번호로 조회하고 이번 주차 출석을 제출한다.
2. 제출 값은 서버에서 다시 검증된 뒤 출석 응답 시트의 **헤더 바로 아래(2행)** 에 삽입된다.
3. 교육 프로젝트가 **화요일 17:00~18:00에 한 번** 실행되어, 타임스탬프 커서 이후의 새 응답만
   오래된 순으로 교육 출석 현황에 반영하고 결과를 메일과 숨김 로그로 남긴다.
4. 등록 프로젝트가 월요일에 교육 출석을 기준으로 등록 명단의 군·팀·전화번호를 보정하고
   군 현황판을 다시 쓴다. 사람이 직접 고친 값은 보호한다.
5. 등록 프로젝트가 금요일에 등록 명단과 교육 출석을 전화번호로 결합해 수료현황을 다시 쓰고
   전체 관리자와 군 담당자에게 주간 통계를 보낸다.
6. 교육 프로젝트가 토요일에 문자공지 대상 명단과 번호 목록을 메일로 보낸다.
7. 집중교육 신청은 위 흐름과 분리된 독립 프로젝트로, Form 제출 즉시 관리자 명단과 공개
   확인 명단을 갱신한다.

## 5. 기능 요구사항

ID 규칙: `REQ-<CATEGORY>-NNN`. CATEGORY는 대문자·숫자, NNN은 세 자리.
한 번 부여한 ID는 재사용하거나 의미를 바꾸지 않는다. 삭제한 ID를 다른 기능에 돌려쓰지 않는다.

### REQ-ATTEND-001

#### 목적
출석 웹앱이 잘못된 입력과 중복·변조된 제출을 시트에 남기지 않게 한다.

#### 동작
- 조회는 이름과 휴대폰 번호가 **모두** 일치하는 기존 응답에서 가장 높은 주차를 돌려준다.
- 제출 값은 클라이언트 검증을 신뢰하지 않고 서버에서 다시 검증한다. 이름(30자 이하),
  휴대폰 번호(10~11자리), 주차(1~4), 군(허용 목록)을 확인한다.
- 1주차 제출은 유입 경로·성별·나이를 모두 요구한다. 2주차 이상은 기존 이력에서 성별·나이를
  가져오고, 이력이 없으면 접수하지 않는다.
- 접수 가능한 주차는 **기존 최고 주차 + 1** 하나뿐이다.
- 이미 기록된 주차를 다시 제출하면 새 행을 만들지 않고 중복으로 응답한다.
- 제출 전 구간은 스크립트 잠금으로 감싸 연속 클릭·동시 요청이 중복 행을 만들지 못하게 한다.
- 화면에 사용자 이름을 넣을 때 HTML을 이스케이프한다.

#### 예외 처리
검증 실패·잠금 획득 실패·이력 불일치는 모두 예외로 중단하고 시트를 바꾸지 않는다.
사용자에게는 원인을 알 수 있는 메시지를 보여 준다.

#### 관련 구현
`attendance-webapp/Code.gs`, `attendance-webapp/Index.html`

#### 관련 테스트
TEST-ATTEND-001

### REQ-ATTEND-002

#### 목적
운영자가 보는 응답 시트를 항상 최신순으로 유지한다.

#### 동작
- 신규 응답은 시트 끝이 아니라 **헤더 바로 아래 2행**에 삽입한다.
- 삽입한 행의 타임스탬프 열에 시트와 동일한 날짜 형식을 적용한다.
- 과거 응답을 한 번에 최신순으로 정렬해야 할 때는 `sortAttendanceSourceNewestFirst`를
  1회 수동 실행한다. 정기 실행이 시트 전체를 다시 정렬하지 않는다.
- 삽입 위치가 바뀌어도 교육 반영은 행 번호가 아닌 타임스탬프를 기준으로 하므로
  누락이 생기지 않아야 한다(REQ-EDU-001).

#### 예외 처리
삽입 실패 시 예외가 전파되고 잠금을 해제한다. 행 삽입·값 쓰기·서식 지정은 별도 호출이며,
실패 시 부분 기록의 자동 롤백은 보장하지 않는다.

#### 관련 구현
`attendance-webapp/Code.gs`

#### 관련 테스트
TEST-ATTEND-002

### REQ-EDU-001

#### 목적
같은 응답을 두 번 반영하거나 새 응답을 빠뜨리지 않고 교육 출석 현황을 갱신한다.

#### 동작
- 처리 기준은 **타임스탬프 커서**다. 커서보다 늦은 응답만 처리한다.
- 응답 시트가 최신순이어도 처리 순서는 타임스탬프 오름차순이다. 1주차부터 차례로 반영한다.
- 실제 실행(dry-run이 아닌 경우)에만 커서를 이번 실행에서 본 최신 타임스탬프로 갱신한다.
- 과거의 행 번호 커서가 남아 있으면 해당 행의 타임스탬프로 환산해 이어받고 이후 삭제한다.
- 커서가 없고 환산도 불가능하면 아무것도 처리하지 않고 초기화가 필요하다고 보고한다.
- 미리보기 함수는 시트·속성·메일을 전혀 바꾸지 않고 예정 건수만 돌려준다.

#### 예외 처리
원본 시트를 찾지 못하면 예외로 중단한다. 새 응답이 0건이면 정상 종료하며 메일을 보내지
않는다.

#### 관련 구현
`education-project/교육 출석 현황 업데이트.gs`

#### 관련 테스트
(없음 — 로컬 정적 테스트가 커버하지 않는다)

### REQ-EDU-002

#### 목적
사람을 잘못 매칭해 남의 출석을 기록하거나 기존 값을 덮어쓰는 일을 막는다.

#### 동작
- 자동 매칭 기준은 **정규화된 전화번호가 교육 시트에서 정확히 한 행과 일치할 때**뿐이다.
- 같은 전화번호가 여러 행에 있으면 반영하지 않고 검토 대상으로 분류한다.
- 2주차 이상인데 전화번호로 찾지 못하면, 이름이 일치하더라도 자동 반영하지 않고 검토
  대상으로 분류한다.
- 신규 추가는 1주차 응답에 한하며, 일요일이 아닌 시각에 제출된 1주차는 예배 구분을 알 수
  없으므로 자동 추가하지 않고 검토 대상으로 둔다.
- 해당 주차 칸에 이미 값이 있으면 덮어쓰지 않고 중복으로 분류한다.
- 군·팀은 최신 응답 값으로 갱신하고 변경 내역을 정보변경으로 기록한다.
- 필수값 누락, 허용되지 않은 주차는 검토 대상으로 분류한다.
- 실행 결과는 신규 추가 / 출석 반영 / 중복 / 정보변경 / 검토 필요로 나누어 메일로 보고한다.

#### 예외 처리
검토·중복은 실패가 아니라 정상적인 부분 성공이다. 시트는 판단 가능한 행만 바꾸고 나머지는
원본을 보존한 채 보고한다.

#### 관련 구현
`education-project/교육 출석 현황 업데이트.gs`

#### 관련 테스트
(없음 — 로컬 정적 테스트가 커버하지 않는다)

### REQ-EDU-003

#### 목적
집중교육 참석자를 일반 교육 출석 현황에 반영하되 기존 기록을 훼손하지 않는다.

#### 동작
- 집중교육 시트에서 참석 표시가 있는 행만 대상으로 한다.
- 전화번호로 교육 출석 현황과 매칭한다. 여러 행이 걸리면 반영하지 않고 충돌로 기록한다.
- 매칭되는 행이 없으면 1~3주차를 완료 표시한 새 행으로 추가한다.
- 매칭되는 행이 있으면 **비어 있는 주차 칸만 채운다.** 이미 값이 있는 칸은 그대로 둔다.
- 4주차 칸에 다른 값이 이미 있으면 보존하고 충돌로 기록한다.
- 이 기능은 정기 트리거가 아니라 필요할 때 수동 실행한다. 미리보기 함수로 먼저 확인한다.
- `previewIntensiveTraining`은 시트를 바꾸지 않는다. 승인 후 실행하는
  `applyIntensiveTrainingNow`는 **실제 시트를 변경**한다. 이 함수는 메일을 보내지 않으며
  `run*Test` 계열이 아니므로, 이름만 보고 미리보기로 판단하지 않는다.

#### 예외 처리
집중교육 시트를 찾지 못하면 예외로 중단한다. 충돌은 실패가 아니라 검토 항목으로 보고한다.

#### 관련 구현
`education-project/집중교육 출석 현황 업데이트.gs`

#### 관련 테스트
(없음 — 로컬 정적 테스트가 커버하지 않는다)

### REQ-NOTI-001

#### 목적
주말 문자공지 담당자가 대상자와 번호를 손으로 추리지 않게 한다.

#### 동작
- 두 종류의 명단을 만든다. **교육 진행 중**(교육 출석 현황에 있고 4주차를 완료하지 않은
  사람)과 **교육 미진행**(수료현황에 등록됐지만 1주차를 시작하지 않은 사람).
- 날짜가 해석되는 기록은 최근 15주와 고정 시작일 중 늦은 날짜를 하한으로 삼는다.
  날짜를 해석하지 못한 행은 다른 제외 조건에 해당하지 않으면 포함될 수 있다.
- 문자 제외 표시가 있는 사람은 두 명단 모두에서 뺀다.
- 이름 또는 전화번호가 없는 행은 제외한다.
- 문자 발송용 번호 목록은 전화번호 기준으로 중복을 제거하고 임원 번호를 포함하며,
  20명 단위로 구분선을 넣어 복사하기 쉽게 만든다.
- 메일 발송은 교육 프로젝트의 공용 메일 관문을 사용한다. 이 파일이 자체 운영 수신자 목록을
  따로 들고 있으면 안 된다(REQ-MAIL-001).

#### 예외 처리
수료현황 시트를 찾지 못하면 예외로 중단한다.

#### 관련 구현
`education-project/문자 명단 리스트.gs`

#### 관련 테스트
(없음 — 대상자 선정 기준을 검증하는 테스트가 없다)

### REQ-REG-001

#### 목적
등록 명단의 군·팀·전화번호를 교육 출석의 최신 값에 맞추되, 사람이 손으로 고친 값을
자동화가 되돌리지 않게 한다.

#### 동작
- 등록 명단의 각 행을 교육 출석 원본 응답과 매칭한다. 저장된 매칭 이름, 현재 이름과
  전화번호, 등록 명단에서 유일한 이름 순으로 판단하며 최신 응답을 우선한다.
- 군·팀·전화번호를 보정한다. **이름은 자동 보정하지 않는다** — 등록 명단의 이름과 교육
  출석 이름이 다르면 검토 내역으로 보고하고 사람이 어느 쪽이 맞는지 확인한다.
- 값이 다르면 교육 최신값으로 보정하고, 보정한 값과 직전 값을 스크립트 속성에 기록한다.
- 다음 실행에서 현재 값이 **직전에 자동 보정한 값과 다르면** 사람이 고친 것으로 판단해
  보호 대상으로 전환하고 이후 자동 수정하지 않는다.
- 이미 보호 대상인 항목은 차이가 있어도 건드리지 않고 보호 내역으로만 보고한다.
- 결과 메일에는 등록자·매칭·미매칭·자동수정·수동수정 보호·검토 건수와, 자동수정 상세
  (행/이름/항목/기존값→교육 최신값/판단 근거)를 담는다.
- 결과 메일 수신 범위는 **정기 실행도 테스트 수신자 한 명**이다. 관리자 전체 발송으로
  전환하려면 그 자체를 사양 변경으로 다룬다.
- 미리보기와 테스트 실행은 시트를 바꾸지 않는다.

#### 예외 처리
매칭되지 않는 등록자는 오류가 아니라 미매칭으로 집계해 보고한다.

#### 관련 구현
`registration-project/등록새가족-군현황 자동 배치.gs`

#### 관련 테스트
TEST-REG-002

### REQ-REG-002

#### 목적
군 현황판을 등록 명단에서 다시 만들고, 연도가 없는 날짜 표기를 해가 바뀌어도 맞게 해석한다.

#### 동작
- 등록 명단을 읽어 **군별로만** 집계해 군 현황판 시트를 다시 쓴다. 예배 구분(4부/5부)은
  군 배정을 나누지 않으므로, 같은 군이면 4부 등록자와 5부 등록자가 한 열에 함께 쌓인다.
- 현황판 열 구성은 `날짜 / 신·조·명·총·영·석·전·슬·임 / 스스로(4부)·스스로(5부) / 미배정 /
  합계`의 14열이다. 1~2행 머리글도 자동화가 이 구성에 맞춰 다시 쓴다.
- **명단을 먼저 쓰고 머리글을 나중에 쓴다.** 머리글의 병합·서식은 보기 위한 것이고 명단이
  본체이므로, 머리글 쪽 실패가 명단 기록을 막으면 안 된다.
- **머리글에 세로 병합을 쓰지 않는다.** 시트에 필터가 걸려 있으면 1행이 필터 머리글이 되어
  Google Sheets가 세로 병합을 거부한다. 단일 열 제목(`날짜`·`미배정`·`합계`)은 병합 대신
  1·2행에 모두 적는다. 가로 병합(군 묶음, 스스로 묶음)은 시도하되 거부되면 건너뛴다.
- **"스스로"만 예배 구분에 따라 4부 열과 5부 열로 나눈다.** 가시적으로 구분해야 하는 값은
  이것뿐이다.
- 군이 비어 있고 소개자가 "스스로"이거나 군배정필요 표기이면 "스스로"로 취급한다.
- 군 값은 첫 글자로 정규화해 9개 군에 맞춘다("신군"·"신1팀" → 신). "모르겠"이 포함된 값은
  군으로 인정하지 않는다.
- **현황판에서 누락되는 등록자가 없어야 한다.** 군을 확정하지 못한 사람과 "스스로"인데 예배
  구분이 4도 5도 아닌 사람은 `미배정` 열에 이름을 쓰고, 동시에 검토 내역으로도 보고한다.
  예배 구분이 4·5가 아니어도 군을 알 수 있으면 그대로 해당 군 열에 배정한다.
- `M/d` 형태의 등록일은 연도가 없으므로 **회기(3월 시작) 기준으로 오늘 날짜에서 추정**한다.
  12월 값은 오늘이 12월이 아니면 전년도로 본다. 연도를 코드에 고정하지 않는다.

#### 예외 처리
등록 시트나 현황판 시트를 찾지 못하면 예외로 중단한다. 이름이 있으나 등록일을 해석할 수 없는
행은 배치할 날짜가 없으므로 현황판에 쓰지 않고 검토 내역으로만 보고한다.
머리글의 병합 실패는 실행 로그에만 남기고 중단하지 않는다 — 꾸미기일 뿐이다. 그 밖의 머리글
실패는 감추지 않고 예외로 올리되, 그 시점에는 명단이 이미 기록돼 있어야 한다.

#### 관련 구현
`registration-project/등록새가족-군현황 자동 배치.gs`

#### 관련 테스트
TEST-REG-001, TEST-REG-003, TEST-REG-005

### REQ-REG-003

#### 목적
담당자가 **직접 군을 정해 줘야 하는 사람**을 매주 메일로 받아, 현황판을 열어 보지 않아도
누가 남아 있는지 알 수 있게 한다.

#### 동작
- 군 현황판을 갱신할 때 다음 두 부류를 하나의 명단으로 모은다.
  - `스스로` — 스스로 열(4부 또는 5부)에 배치된 사람.
  - `미배정` — 미배정 열에 배치된 사람과, 등록일을 해석하지 못해 현황판에 배치하지 못한 사람.
- 명단의 각 항목은 구분·등록일·예배 구분·등록 시트 행 번호·이름·군 표기 원문·소개자를 담는다.
  행 번호를 넣는 이유는 받는 사람이 등록 시트에서 바로 그 행을 찾아 고치기 위해서다.
- 이 명단은 등록 유지보수 결과 메일의 **독립된 섹션**으로 텍스트·HTML 본문 모두에 들어간다.
  검토 필요 상세에 섞어 두지 않는다 — 스스로 등록은 오류가 아니라 처리 대기이기 때문이다.
- 메일 제목에 명단 인원 수를 넣어 열어 보지 않아도 건수를 알 수 있게 한다.
- 수신자는 REQ-REG-001과 같다. 별도의 수신자 목록을 만들지 않는다.
- 명단이 비면 섹션을 만들지 않는다.

#### 예외 처리
명단 집계는 현황판 갱신의 부수 결과이므로 별도 실패 경로가 없다. 현황판 갱신이 중단되면
이 명단도 만들어지지 않는다.

#### 관련 구현
`registration-project/등록새가족-군현황 자동 배치.gs`

#### 관련 테스트
TEST-REG-004

### REQ-REPORT-001

#### 목적
등록 명단과 교육 출석을 결합한 수료현황과 주간 통계를 담당자에게 정해진 시각에 전달한다.

#### 동작
- 등록 명단과 교육 출석 현황을 **전화번호 기준**으로 결합해 수료현황 시트를 다시 쓴다.
- 등록 시트의 군·팀·전화번호는 이 과정에서 자동으로 덮어쓰지 않는다. 불일치는 보고만 한다.
- 전체 관리자에게 전체 통계 메일을, 군 담당자에게 각 군 통계 메일을 보낸다.
- 미리보기와 테스트 실행은 시트를 바꾸지 않으며, 테스트는 메일을 한 통만 보낸다.
- 실행 결과는 수료 자동화 로그에 기록한다(REQ-LOG-001).

#### 예외 처리
필요한 시트를 찾지 못하면 예외로 중단한다. 미매칭·중복·불일치는 집계해 보고한다.

#### 관련 구현
`registration-project/등록 새가족 새가족교육 수료현황 자동화.gs`

#### 관련 테스트
(없음 — 로컬 정적 테스트가 커버하지 않는다)

### REQ-SETTLE-001

#### 목적
상반기 등록 새가족 현황을 군·날짜·일반/행축 구분으로 집계해 목표 대비 실적을 한 화면에
보여 준다.

#### 동작
- 원본은 등록 명단, 출력은 `상반기 결산` 시트다. 둘 중 하나라도 없으면 중단한다.
- 결산 시트 1행의 날짜 표기와 2행의 구분(`일반`/`행축`)을 읽어 `날짜_구분` → 열 위치를
  만든다. 1행이 빈 칸이면 병합 셀을 감안해 직전 날짜를 이어 쓴다.
- 군은 등록 명단의 군 첫 글자를 9개 군(신·조·명·총·영·석·전·슬·임) → 3~11행으로 매핑한다.
- 등록일은 회기 기준으로 연도를 추정한 `M/d` 표기로 정규화한다. 날짜가 `3/8` 이전이거나
  12월~2월이면 `행축` 판정 대상이 아니다.
- 유입 경로가 `관계` 또는 `설문지`이고 `3/8` 이후면 `행축`, 그 외는 `일반`으로 집계한다.
- 다시 계산하기 전에 군 행의 계산 영역(일반·행축 합계와 비율, 날짜별 열)과 12행 합계를
  지운다. **목표값 열은 건드리지 않는다.**
- 12행에 군별 합계와 전체 목표 대비 비율을 쓰고, 비율 셀에는 백분율 서식을 적용한다.
- 군이나 날짜/구분 열을 찾지 못한 행은 집계하지 않고 검토 내역으로 보고한다.
- `previewSettlementReport`는 시트를 바꾸지 않는다. `generateSettlementReport`가 실제
  반영 경로이며, 등록 자동화가 운영 모드에서 비활성이면 중단한다.
- 정기 트리거는 없다. 필요할 때 수동 실행한다.

#### 예외 처리
필요한 시트를 찾지 못하면 예외로 중단한다. 검토 내역은 실패가 아니라 집계 제외 항목이다.

#### 관련 구현
`registration-project/제목 없음.gs`

#### 관련 테스트
TEST-SETTLE-001

### REQ-INTENSIVE-001

#### 목적
집중교육 신청을 Form 한 번 제출로 접수하고, 전화번호를 공개하지 않으면서 신청 현황을
누구나 확인할 수 있게 한다.

#### 동작
- 군은 9개이며, 군을 선택하면 해당 군의 팀 Section으로만 분기해 잘못된 군·팀 조합을 만들 수
  없게 한다.
- Form 표기(`명군`)와 시트 저장 표기(`명`)를 변환하고, 정렬할 때 원래 표기로 복원한다.
- 전화번호는 숫자만 입력해도 `010-xxxx-xxxx` 형식으로 정규화하며 11자리만 허용한다.
- 제출 시 관리자 명단에는 오류 상태도 기록하고, 필수값·군·팀·전화번호가 유효한 응답만
  공개 명단에 추가한다. 공개 명단은 군 → 팀 → 참석자 이름 순으로 정렬한다.
- 참석자 이름과 전화번호가 모두 같은 이전 신청은 관리자 명단에 중복 의심으로 표시한다.
- 트리거는 Form이 아니라 **관리자 Spreadsheet의 제출 이벤트**에 설치한다.
- 최초 설치 함수는 폴더·Form·Spreadsheet 2개 생성, 전체 분기 구성, 공유 권한, 트리거까지
  스스로 검증한다. 실패 시 속성·해당 제출 트리거를 정리하고 생성 파일·폴더를
  휴지통으로 이동하려 시도한다. 정리 실패는 로그에 남기므로 완전한 롤백을 보장하지 않는다.

#### 예외 처리
설치 검증 실패는 정리를 시도한 후 중단한다. 기존 설치 속성이 있으면 재생성하지 않는다.
재설치는 속성·트리거 초기화 후 수행하며 기존 Drive 파일은 별도 관리한다.

#### 관련 구현
`intensive-training-application/Code.gs`

#### 관련 테스트
TEST-INTENSIVE-001

### REQ-MAIL-001

#### 목적
테스트 목적의 실행이 운영 수신자 전체에게 메일을 보내는 사고를 구조적으로 막는다.

#### 동작
- 모든 메일은 프로젝트별 공용 발송 관문 하나를 거친다. 개별 기능이 `MailApp`을 직접
  호출하지 않는다.
- `forceTestRecipient`가 켜져 있거나 모드가 운영이 아니면 수신자를 **테스트 수신자 한 명으로
  고정**하고 제목 앞에 `[TEST]`를 붙인다. 호출자가 운영 수신자 목록을 직접 넘겨도 이 규칙이
  이긴다.
- 테스트 강제 상태에서 최종 수신자가 정확히 그 한 명이 아니면 발송하지 않고 예외로 중단한다.
- 수신자 목록은 공백 제거 후 중복을 제거한다.
- 운영 수신자 목록은 프로젝트당 설정 객체 한 곳에서만 관리한다. 다른 파일이 자체 목록을
  중복 보유하지 않는다.
- 메일 하단의 수신자 안내 문구는 실제 수신 범위와 일치해야 한다. 운영 발송에 "한 명에게만
  발송" 같은 문구를 쓰지 않는다.
- 메일을 보내는 교육 테스트·문자공지 테스트·등록 유지관리 테스트·수료보고 테스트는
  이 안전장치를 켠다. 집중교육 반영은 `applyIntensiveTrainingNow`가 메일 없이 실제 시트를
  변경한다.

#### 예외 처리
안전장치 위반은 발송 실패가 아니라 실행 중단 사유다.

#### 관련 구현
`education-project/교육 출석 현황 업데이트.gs`,
`registration-project/등록새가족-군현황 자동 배치.gs`

#### 관련 테스트
TEST-MAIL-001, TEST-MAIL-002

### REQ-LOG-001

#### 목적
실행 결과를 추적할 수 있게 남기되 운영 화면을 어지럽히지 않는다.

#### 동작
- 교육 증분 처리·등록 유지관리·수료보고의 기록 경로는 요약을 로그 시트 2행에 최신순으로 쓴다.
  교육 신규 응답이 없거나 커서 초기화가 필요하면 기록 전에 반환한다.
- 다른 보이는 시트가 있으면 새 로그 시트와 기존의 보이는 로그 시트를 숨긴다.
  이때 첫 번째 보이는 운영 시트를 활성화하므로 사용자가 보던 탭의 보존은 보장하지 않는다.
- 로그 기록은 dry-run이 아닌 실제 실행에서만 수행한다.
- 로그 기록 함수를 비활성 stub으로 두지 않는다. 기록을 끄는 것은 사양 변경이다.

#### 예외 처리
로그 기록 실패는 예외로 전파되며 이미 수행한 시트 변경을 되돌리지 않는다.

#### 관련 구현
`education-project/교육 출석 현황 업데이트.gs`,
`registration-project/등록새가족-군현황 자동 배치.gs`,
`registration-project/등록 새가족 새가족교육 수료현황 자동화.gs`

#### 관련 테스트
TEST-LOG-001

## 6. 비기능 요구사항

### NFR-SEC-001

개인정보와 인증 정보는 저장소 밖에 둔다.

- 개인 명단, 실행 결과, 시트 데이터, clasp 인증 파일을 저장소·Knowledge·Evidence·커밋
  메시지에 넣지 않는다. `.gitignore`는 clasp·환경변수 파일과 일부 도구 산출물을 제외한다.
  임의의 개인정보 파일까지 자동 차단하지 않으므로 커밋 대상을 별도로 확인한다.
- 집중교육 관리자 파일과 시스템 폴더는 소유자 전용 비공개다. 공개 확인 시트만 링크 뷰어
  권한을 갖는다.
- 공개 확인 시트의 열은 번호·군·팀·참석자 이름·신청자 이름으로 한정한다. 전화번호·이메일·계정·
  응답 ID를 포함하지 않는다.
- 교육 결과 상세의 전화번호는 마스킹한다. 문자공지와 등록 보정 상세 등 전체 번호가
  포함되는 보고도 있으므로 메일 수신 범위를 공용 발송 관문으로 제한한다.

#### 관련 테스트
TEST-SEC-001

### NFR-OPS-001

정기 실행은 예측 가능하고, 겹쳐 실행되지 않으며, 한 줄로 멈출 수 있어야 한다.

- 설치형 트리거는 주 단위 고정 일정으로 동작한다. Apps Script가 지정 시간대 안에서 실행
  시각을 고르므로 정각 실행은 보장되지 않는다.
- 운영 일정은 교육 출석 반영 **화요일 17:00~18:00 한 번**, 등록 유지관리 월요일
  08:00~09:00, 등록 수료·주간 통계 금요일 11:00~12:00, 문자공지 토요일 08:00~09:00이다.
  교육 출석 반영은 하루 한 번이면 충분하므로 금요일 실행을 두지 않는다.
- 교육·등록 정기 트리거는 스크립트 잠금 안에서 실행한다. 30초 안에 잠금을 얻지
  못하면 작업하지 않고 중단한다. 출석 웹앱 제출은 10초를 기다린다.
- 설정의 `active`가 거짓이면 트리거 진입점은 어떤 데이터도 바꾸지 않고 즉시 종료한다.
- 함수명을 바꾸면 트리거가 끊어진다. 기존 트리거 함수명은 호환용 래퍼로 유지한다.

#### 관련 테스트
(없음)

### NFR-DATA-001

개인정보를 담은 원본·로그·보정 상태는 자동으로 삭제하지 않는다.

- 출석 응답 원본, 숨김 로그 시트, 등록 보정 상태 속성의 보존 기간을 지금은 정하지 않는다.
  자동 삭제·자동 만료 코드를 두지 않는다.
- 정리가 필요해지면 삭제 주체·주기·백업 여부를 이 사양에 먼저 적고 구현한다. 저장소 밖
  시트를 임의로 지우지 않는다.
- 보정 상태 속성에는 개인정보 값과 수동 수정 보호 정보가 들어 있으므로, 정리할 때는 보호
  상태를 잃지 않는 방법을 함께 정한다.

#### 관련 테스트
(없음 — 보존 정책은 코드로 검증하지 않는다)

### NFR-PERF-001

Apps Script 실행 시간과 호출 한도 안에서 동작해야 한다.

- 교육 출석·군 현황판·수료현황은 배열로 일괄 쓴다. 등록 자동 보정은 변경 셀을 개별 기록한다.
- 스크립트 속성은 등록자 수만큼 개별 조회·저장하지 않는다. 한 번에 읽고 한 번에 저장한다.
- 응답 시트 전체를 읽되 커서 이후 응답만 반영한다(REQ-EDU-001).

#### 관련 테스트
TEST-PERF-001

### NFR-SYNC-001

저장소 코드가 운영 Apps Script와 같아야 하며, 다르면 드러나야 한다.

- 네 프로젝트의 script ID는 매니페스트 한 곳에서 관리한다(`scripts/apps-script-projects.json`).
- `diff`로 운영과 로컬 파일의 동일·차이·한쪽에만 존재하는 상태를 확인한다.
- `push`는 해당 프로젝트의 파일 전체를 교체한 뒤 **다시 읽어 일치를 검증**하고, 불일치가
  남으면 실패로 보고한다.
- 웹앱은 버전 고정 배포이므로 코드 반영만으로 사용자 화면이 바뀌지 않는다. 새 버전을 만들어
  기존 배포에 연결해야 한다.
- 인증은 clasp가 저장한 refresh token을 재사용한다. 토큰 파일을 저장소에 두지 않는다.

#### 관련 테스트
(없음)

## 7. 데이터 사양

이 자동화가 다루는 개인 데이터 범주는 **성명, 휴대폰 번호, 성별, 나이, 소속 군·팀, 등록일,
교육 출석일, 유입 경로**다. 값 자체는 이 문서와 저장소 어디에도 쓰지 않는다.

| 데이터 | 위치 | 성격 |
|---|---|---|
| 교육 출석 원본 응답 | 출석 응답 스프레드시트 | 웹앱·Form이 append. 최신순 정렬 |
| 교육 출석 현황 | 교육관리 스프레드시트 | 자동화가 갱신하는 기준 명단 |
| 집중교육 참석 | 교육관리 스프레드시트 | 수동 병합 입력 |
| 등록 명단·군 현황·수료현황·결산 | 등록 스프레드시트 | 자동화가 갱신 |
| 집중교육 신청(관리용) | 비공개 관리자 스프레드시트 | 전화번호 포함 |
| 집중교육 신청(공개) | 공개 확인 스프레드시트 | 전화번호 없음 |
| 실행 요약 로그 | 각 스프레드시트의 숨김 로그 시트 | 최신순 append |
| 처리 커서·보정 상태 | Apps Script 스크립트 속성 | 타임스탬프, 매칭 키, 이전·자동 보정값, 수동 보호 상태 |

보존 기간은 정하지 않으며 자동 삭제도 하지 않는다(NFR-DATA-001). 군 현황판은 1~2행 머리글과
3행부터의 14열을 재작성하며, 이전 20열 배치가 남긴 3행 이하의 내용과 배경색을 먼저 지운다.
수료현황은 2행부터 15열의 기존 내용을 지운 뒤 재작성한다. 수료현황의 문자 제외·미확인·비고는 기존 전화번호가
정확히 한 행에 대응할 때만 보존한다. 보정 상태 속성에도 개인정보 값이 포함될 수 있다.

## 8. Configuration 사양

| 항목 | 위치 | 기본값 | 설명 |
|---|---|---|---|
| `active` | 교육·등록 설정 객체 | `true` | 거짓이면 트리거가 아무 작업도 하지 않는다 |
| `mode` | 교육·등록 설정 객체 | `PRODUCTION` | `TEST`면 테스트 수신자 한 명에게만 발송 |
| `testRecipient` | 교육·등록 설정 객체 | 필수 | 안전장치가 고정하는 단일 수신자 |
| 운영 수신자 목록 | 교육·등록 설정 객체 | 필수 | 프로젝트당 한 곳에서만 관리 |
| `groups` | 등록 설정 객체 | 9개 군 약칭 | **회기마다 바뀐다.** 이 배열의 순서가 군 현황판 열 순서이고, 열 수는 `군 수 + 5`로 계산된다. 등록·보고 프로젝트의 군 정의는 여기 한 곳뿐이다 (8.1) |
| `groupRecipients` | 등록 설정 객체 | 군별 담당자 | 키 목록이 `groups`와 같아야 한다. 어긋나면 테스트가 실패한다 |
| 스프레드시트·시트 이름 | 각 설정 객체 | 필수 | 운영 Runtime contract. 임의 변경 금지 |
| 처리 커서 속성 이름 | 교육 설정 객체 | 필수 | 타임스탬프 커서와 레거시 행 커서 |
| 예배 구분 기준 시각 | 교육 설정 객체 | 13:30 | 이전은 4부, 이후는 5부 |
| 조회 기간·고정 시작일 | 문자공지 설정 객체 | 15주 / 고정일 | 문자공지 대상 조회 범위 |
| 허용 주차·군·성별 | 웹앱 설정 객체 | 1~4주차 / 군 목록 | 서버 검증 기준 |
| script ID 매니페스트 | `scripts/apps-script-projects.json` | 필수 | 동기화 대상 네 프로젝트 |
| clasp refresh token | 사용자 홈의 clasp 설정 파일 | 필수 | 저장소에 두지 않는다 |

웹앱·교육·등록의 공통 설정과 문자공지 설정은 `Object.freeze`를 사용한다.
집중교육 신청 설정은 일반 객체이며, 동결은 중첩 배열까지 불변임을 뜻하지 않는다.

### 8.1 회기 전환 시 군 구성 변경 절차

군 구성은 **회기(3월 시작)마다 바뀐다.** 새 회기는 이전 회기 스프레드시트를 복사해 시작하며,
이전 회기 스프레드시트와 그 데이터는 그대로 보존한다. 군 이름은 네 Apps Script 프로젝트에
걸쳐 있으므로 **아래 순서대로 전부 고친다.** 한 곳이라도 빠지면 조용히 틀린다 — 자동화가
멈추지 않고 사람만 없어진 것처럼 보이기 때문에 알아차리기 어렵다.

#### 코드에서 고치는 자리

| 순서 | 프로젝트 | 위치 | 고치는 것 | 빠뜨리면 |
|---|---|---|---|---|
| 1 | 등록·보고 | `등록새가족-군현황 자동 배치.gs`의 `REGISTRATION_AUTOMATION.groups` | 군 약칭 배열. **배열 순서 = 현황판 열 순서** | 새 군 등록자가 전부 미배정 열로 간다 |
| 2 | 등록·보고 | 같은 파일의 `groupRecipients` | 군 → 담당자 메일. 키를 1과 정확히 같게 | 군별 통계 메일이 가지 않는다 |
| 3 | 등록·보고 | `제목 없음.gs`의 결산 군 행 맵 | 결산 시트의 군별 행 번호 | 결산 집계가 다른 군에 쌓인다 |
| 4 | 교육 관리 | `교육 출석 현황 업데이트.gs`의 유효 군 목록 | 군 첫 글자 허용 목록 | 새 군 출석이 전부 검토로 빠진다 |
| 5 | 출석 웹앱 | `attendance-webapp/Code.gs`의 허용 군 목록 | 서버 검증 목록(`신군` 형태) | 제출이 거부된다 |
| 6 | 출석 웹앱 | `attendance-webapp/Index.html`의 군 `<option>`과 군→팀 매핑 | 화면 선택지 | 선택지에 없어 입력 자체가 안 된다 |
| 7 | 집중교육 신청 | `intensive-training-application/Code.gs`의 `GROUPS`와 군→팀 맵 | Form 생성 기준 | Form을 다시 만들면 이전 회기 군이 나온다 |

1과 2는 등록·보고 프로젝트 안에서 군을 정의하는 **유일한** 자리다. 현황판 열 번호와 열 수,
군 표기 정규화는 모두 1에서 계산되므로 **열 번호를 손으로 세지 않는다.**

#### 시트에서 하는 일

- 새 회기용으로 스프레드시트를 복사한 뒤, 각 프로젝트 설정의 스프레드시트 ID
  (`registrationSpreadsheetId`, `educationSpreadsheetId`, `attendanceSpreadsheetId`)를
  복사본 ID로 바꾼다. 이 값을 바꾸지 않으면 새 회기 작업이 이전 회기 시트를 고친다.
- 복사본의 `등록 새가족` 시트에서 이전 회기 명단을 비우고, `등록 새가족 군 현황`의 3행 이하를
  비운다. **1~2행 머리글은 손대지 않는다** — 첫 실행이 새 군 구성에 맞춰 다시 쓴다(REQ-REG-002).

#### 확인

```bash
node tests/registration-automation.test.js      # 1과 2가 어긋나면 실패한다
node tests/attendance-webapp.test.js
node tests/intensive-training-application.test.js
```

고친 프로젝트는 각각 배포해야 반영된다(`scripts/apps-script.mjs`). 출석 웹앱은 코드 배포와
별도로 **웹앱 재배포**가 필요하다.

#### 이전 회기 군으로 남은 데이터

없어진 군으로 등록된 행은 미배정 열로 간다. 이것은 결함이 아니라 의도된 동작이다 —
이름이 현황판에서 사라지지 않고 담당자에게 메일로 보고된다(REQ-REG-002, REQ-REG-003).

## 9. 오류 처리 정책

- 교육 증분 처리의 처리 범위는 저장된 커서로 정한다. 시트·커서·로그·메일은 단일
  트랜잭션이 아니므로 실패 단계에 따라 다음 실행이 복구하지 못하는 작업이 있을 수 있다.
  실패 시 커서와 실제 반영 상태를 확인한다. 트리거 실패 알림은 Apps Script 설정에 위임한다.
- **부분 성공을 허용한다.** 자동 판단이 가능한 행만 반영하고, 나머지는 시트를 바꾸지 않은 채
  검토·중복·충돌로 분류해 메일과 숨김 로그에 남긴다. 검토 항목을 자동으로 추측해 고치지
  않는다.
- **중단해야 하는 조건**은 다음과 같다. 필요한 시트·스프레드시트를 찾지 못한 경우, 잠금
  획득 실패, 메일 수신자 안전장치 위반, 웹앱 입력 검증 실패, 웹앱에서 요청 주차가 이력과
  맞지 않는 경우, `push` 후 재검증 불일치, 집중교육 설치 검증 실패(생성물 정리 시도 후 중단).
- **아무 일도 하지 않고 정상 종료하는 조건**: `active`가 거짓, 새 응답 0건, 처리 기준 커서가
  없어 초기화가 필요한 경우.
- **기록 위치**: 실행 요약은 각 스프레드시트의 숨김 로그 시트, 상세는 결과 메일, 예외와
  진행 로그는 Apps Script 실행 로그.
- 미리보기 함수는 어떤 실패 경로에서도 시트·속성·메일을 바꾸지 않는다.

## 10. 보안 요구사항

- 인증 수단은 두 가지다. Apps Script 실행 계정 권한(시트·메일·드라이브)과, 로컬 동기화
  도구가 재사용하는 clasp refresh token. 토큰 파일은 사용자 홈에만 두고 저장소에서 제외한다.
- 저장소에 들어가면 안 되는 값: 개인 명단, 실행 결과, 인증 토큰, 개인 절대경로.
  스프레드시트·스크립트 식별자는 운영 코드에 필요한 범위에서만 두고 이 사양 문서에는 쓰지
  않는다.
- 출석 웹앱은 익명 접근을 허용하되, 실행 권한은 배포자 계정이며 조회에 이름과 전화번호를
  모두 요구한다. 외부 iframe 임베드는 허용하지 않는다.
- 사용자 입력을 HTML에 넣기 전 이스케이프한다.
- 집중교육 관리자 파일은 비공개, 공개 확인 시트만 링크 뷰어다(NFR-SEC-001).
- 운영 수신자, 시트 식별자·이름·열 위치, 트리거 함수명, `active`, `mode`는 근거 없이 바꾸지
  않는다.

## 11. 테스트 사양

ID 규칙: `TEST-<CATEGORY>-NNN`.

아래 테스트는 모두 `npm test`(`scripts/run-tests.mjs`)로 일괄 실행한다. 전부 Apps Script API를
호출하지 않는 **정적·단위 검증**이며, 실제 시트 권한·트리거·메일 도착은 검증하지 않는다.

### TEST-ATTEND-001

#### 검증 대상
REQ-ATTEND-001

#### 선행 조건
Node.js 18 이상. 별도 패키지 설치 없음.

#### 절차
`tests/attendance-webapp.test.js`를 실행한다. 웹앱 서버 소스를 VM에 로드해 이름·전화번호
정규화, 전화번호 유효성, 주차 파싱을 호출하고, 허용되지 않은 주차(5주차)로 제출 검증을
호출한다. 소스에 스크립트 잠금 사용과 화면 쪽 이스케이프·실패 처리기가 있는지도 확인한다.

#### Expected Result
정규화·유효성 결과가 기대값과 같고, 5주차 제출이 "허용되지 않은 교육 주차" 예외로 거부되며,
잠금·이스케이프·실패 처리기 패턴이 모두 발견된다.

### TEST-ATTEND-002

#### 검증 대상
REQ-ATTEND-002

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/attendance-webapp.test.js`에서 가짜 시트 객체로 삽입 함수를 호출하고 호출 순서를
기록한다. 서버 소스에 시트 끝 append 호출이 남아 있지 않은지도 확인한다.

#### Expected Result
2행 앞에 행을 삽입하고, 2행에 값을 쓰고, 타임스탬프 열에 날짜 형식을 적용하는 순서가
정확히 관찰된다. `appendRow` 호출은 발견되지 않는다.

### TEST-MAIL-001

#### 검증 대상
REQ-MAIL-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/education-automation.test.js`와 `tests/registration-automation.test.js`를 실행한다.
메일 API를 가짜로 바꾼 VM에서 공용 발송 관문을 세 가지 방식으로 호출한다 — 일반 발송,
테스트 강제 발송, 운영 수신자 목록을 직접 넘긴 테스트 강제 발송. 또한 `run*Test` 진입점
소스가 테스트 강제 옵션을 켜는지, 정기 발송 경로는 켜지 않는지 확인한다.

#### Expected Result
일반 발송은 운영 수신자 5명에게 가고 제목에 `[TEST]`가 없다. 테스트 강제 발송은 수신자가
테스트 주소 한 명이고 제목이 `[TEST]`로 시작한다. 운영 목록을 직접 넘겨도 결과는 같다.
`run*Test` 진입점에서 강제 옵션이 발견되고, 토요일 정기 발송은 운영 5명을 유지한다.

### TEST-MAIL-002

#### 검증 대상
REQ-MAIL-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/education-notification.test.js`를 실행한다. 교육 설정과 문자공지 소스를 함께 로드해
발송 관문을 한 번 호출하고, 문자공지 소스가 자체 운영 수신자 목록을 정의하는지 검사한다.

#### Expected Result
수신자가 설정 객체의 운영 목록과 순서까지 같은 5명이고 중복이 없으며, 문자공지 소스에는
별도의 운영 수신자 목록 정의가 없다.

### TEST-LOG-001

#### 검증 대상
REQ-LOG-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/education-automation.test.js`와 `tests/registration-automation.test.js`를 실행한다.
가짜 스프레드시트로 로그 시트 획득 함수를 두 상황에서 호출한다 — 로그 시트가 없을 때,
그리고 이미 있으나 보이는 상태일 때. 또 로그 기록 함수가 비활성 stub으로 되돌아가지
않았는지, 실행 경로에서 실제로 호출되는지를 소스에서 확인한다.

#### Expected Result
새로 만든 로그 시트와 기존에 보이던 로그 시트 모두 숨김 상태가 되고, 활성 시트는 원래
운영 시트로 남는다. 로그 기록 함수는 stub이 아니며 각 실행 경로에서 호출된다.

### TEST-REG-001

#### 검증 대상
REQ-REG-002

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 연도 추정 함수를 여러 기준일로 호출한다 —
9월 실행 중의 12월과 3월, 12월 실행 중의 12월, 해가 바뀐 이듬해 6월 실행 중의 12월과 3월.
연도를 고정한 표현이 소스에 남아 있는지도 확인한다.

#### Expected Result
회기 시작 전 12월은 전년도, 3월은 당해 연도로 해석되고, 12월 실행 중의 12월은 당해 연도다.
해가 바뀌어도 같은 규칙이 적용되며, 연도를 하드코딩한 표현은 발견되지 않는다.

### TEST-REG-002

#### 검증 대상
REQ-REG-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 자동 보정 항목 정의 함수
(`registrationCorrectionFields_`)를 등록 행과 교육 출석 매칭 결과로 호출한다. 반환된 항목의
키 목록과, 이름이 다른 매칭에서 검토 사유가 만들어지는 소스 경로를 확인한다.

#### Expected Result
자동 보정 항목은 군·팀·전화번호 세 개뿐이고 이름은 포함되지 않는다. 이름이 다르면 등록 행을
바꾸지 않고 검토 내역으로 보고하는 코드가 발견된다.

### TEST-REG-003

#### 검증 대상
REQ-REG-002

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 군 현황판 열 배치 함수
(`resolveDashboardGroupColumn_`)를 여러 등록 행 형태로 호출한다 — 4부 "신", 5부 "신",
4부 "임", 예배 구분 없는 "석", 오타 "신군", 빈 군 + 소개자 "스스로"(4부·5부), 예배 구분 없는
"스스로", 빈 군 + 빈 소개자. 머리글 생성 함수의 반환값과 열 구성 상수도 함께 확인한다.

#### Expected Result
같은 군은 예배 구분과 관계없이 같은 열 번호를 받고, "스스로"만 4부·5부가 서로 다른 열을
받는다. 군을 확정할 수 없는 값과 예배 구분 없는 "스스로"는 `assigned: false`와 함께 `미배정`
열 번호를 받으며, 어떤 입력에서도 열 번호가 없는 결과는 나오지 않는다. 머리글 2행에는 9개 군과
`4부`·`5부`가 열 구성 상수와 같은 자리에 놓인다.

### TEST-REG-004

#### 검증 대상
REQ-REG-003

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 합성 등록 명단으로 `updateNewFamilyStatus_`를
실행한 뒤, 반환된 명단의 구성과 각 항목의 필드를 확인한다. 이어서 그 결과로 텍스트·HTML
메일 본문 생성 함수를 호출해 명단의 모든 이름이 두 본문에 실제로 들어가는지 확인한다.
수신자 설정이 한 명으로 고정돼 있는지도 소스에서 확인한다.

#### Expected Result
스스로 열에 배치된 사람과 미배정 사유를 가진 사람이 모두 명단에 들어가고 `스스로`·`미배정`
구분이 맞다. 각 항목에 등록 시트 행 번호와 등록일이 있다. 명단의 모든 이름이 텍스트 메일과
HTML 메일 본문에서 발견된다. 집계만 되고 본문에 들어가지 않는 상태는 실패로 잡힌다.

### TEST-REG-005

#### 검증 대상
REQ-REG-002

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 **1행이 필터 머리글인 가짜 시트**로
`updateNewFamilyStatus_`를 실행한다. 이 가짜 시트의 `merge()`는 필터 머리글을 포함한 세로
병합을 실제 Google Sheets와 같은 문구로 거부한다. 실행이 예외 없이 끝나는지, 모든 등록자가
기록되는지, 세로 병합을 시도한 호출이 있는지, 병합 없이도 2행에 모든 열 제목이 있는지 본다.
이어서 머리글 쓰기가 실패하도록 만든 시트로 다시 실행해, 예외가 올라오면서도 명단은 이미
기록돼 있는지 확인한다.

#### Expected Result
필터가 걸린 시트에서도 예외 없이 끝나고 등록자 전원이 기록되며, 이전 배치의 잔여 내용과
머리글은 남지 않는다. 세로 병합 호출은 0건이고, `날짜`·`미배정`·`합계`가 2행에도 있다.
머리글 쓰기가 실패한 경우에는 예외가 올라오지만 명단은 온전히 남아 있다.
이 테스트는 수정 전 구현에서 실제로 실패해야 한다(운영 사고 재현).

### TEST-SETTLE-001

#### 검증 대상
REQ-SETTLE-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 결산 날짜 정규화와 행축 판정 함수를 여러 입력으로
호출한다 — `3/7`, `3/8`, `5/12`, `12/25`, `2026-05-12` 형식과 빈 값. 목표값 열을 보존하는지,
합계 행과 검토 내역을 쓰는지도 소스에서 확인한다.

#### Expected Result
`3/8` 이후만 행축 후보가 되고 12월~2월은 일반으로 남는다. 연도가 붙은 날짜도 `M/d`로
정규화된다. 목표값 열을 덮어쓰는 코드는 발견되지 않는다.

### TEST-PERF-001

#### 검증 대상
NFR-PERF-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 등록 유지관리 소스의 스크립트 속성 접근 방식을
검사한다.

#### Expected Result
행마다 속성을 개별 조회하는 호출은 없고, 전체를 한 번에 읽는 호출과 한 번에 저장하는
호출이 발견된다.

### TEST-INTENSIVE-001

#### 검증 대상
REQ-INTENSIVE-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/intensive-training-application.test.js`를 실행한다. 군 수와 군별 팀 수, 전화번호
정규화와 형식 검증, 군 표기 변환·복원, 같은 헤더가 반복되는 Section에서의 응답값 선택을
확인하고, 트리거가 관리자 Spreadsheet 제출 이벤트로 설치되는지 소스에서 확인한다.

#### Expected Result
군은 9개이고 군별 팀 수가 기대값과 같다. 숫자만 입력한 번호가 하이픈 형식으로 정규화되고
10자리는 거부된다. 군 표기 변환·복원과 Section별 응답값 선택이 기대값과 같다. Form 이벤트가
아닌 Spreadsheet 제출 이벤트 트리거만 발견된다.

### TEST-SEC-001

#### 검증 대상
NFR-SEC-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/intensive-training-application.test.js`에서 관리자 파일 비공개 설정과 공개 시트 링크
뷰어 설정이 소스에 있는지 확인한다. 헤더 검사는 테스트 안에 정의된 기대 헤더 목록을
대상으로 하므로 실제 생성 시트의 헤더 검증을 대체하지 않는다.

#### Expected Result
관리자 파일 비공개 설정과 공개 시트 링크 뷰어 설정이 모두 발견되고, 기대 헤더 목록에는
전화번호·연락처·이메일·계정·응답 ID에 해당하는 열이 없다.

## 12. 요구사항 추적성

| Requirement | Implementation | Test | Status |
|---|---|---|---|
| REQ-ATTEND-001 | `attendance-webapp/Code.gs` | TEST-ATTEND-001 | implemented |
| REQ-ATTEND-002 | `attendance-webapp/Code.gs` | TEST-ATTEND-002 | implemented |
| REQ-EDU-001 | education-project/교육 출석 현황 업데이트.gs | (없음) | implemented |
| REQ-EDU-002 | education-project/교육 출석 현황 업데이트.gs | (없음) | implemented |
| REQ-EDU-003 | education-project/집중교육 출석 현황 업데이트.gs | (없음) | implemented |
| REQ-NOTI-001 | education-project/문자 명단 리스트.gs | (없음) | implemented |
| REQ-REG-001 | registration-project/등록새가족-군현황 자동 배치.gs | TEST-REG-002 | implemented |
| REQ-REG-002 | registration-project/등록새가족-군현황 자동 배치.gs | TEST-REG-001, TEST-REG-003, TEST-REG-005 | implemented |
| REQ-REG-003 | registration-project/등록새가족-군현황 자동 배치.gs | TEST-REG-004 | implemented |
| REQ-REPORT-001 | registration-project/등록 새가족 새가족교육 수료현황 자동화.gs | (없음) | implemented |
| REQ-SETTLE-001 | registration-project/제목 없음.gs | TEST-SETTLE-001 | implemented |
| REQ-INTENSIVE-001 | `intensive-training-application/Code.gs` | TEST-INTENSIVE-001 | implemented |
| REQ-MAIL-001 | education-project/교육 출석 현황 업데이트.gs, registration-project/등록새가족-군현황 자동 배치.gs | TEST-MAIL-001, TEST-MAIL-002 | implemented |
| REQ-LOG-001 | education-project/교육 출석 현황 업데이트.gs, registration-project/등록새가족-군현황 자동 배치.gs | TEST-LOG-001 | implemented |
| NFR-SEC-001 | `intensive-training-application/Code.gs`, `.gitignore` | TEST-SEC-001 | implemented |
| NFR-OPS-001 | education-project/교육 출석 현황 업데이트.gs, registration-project/등록새가족-군현황 자동 배치.gs | (없음) | implemented |
| NFR-DATA-001 | 운영 Sheets·스크립트 속성 (구현 없음 — 자동 삭제 금지) | (없음) | implemented |
| NFR-PERF-001 | registration-project/등록새가족-군현황 자동 배치.gs | TEST-PERF-001 | implemented |
| NFR-SYNC-001 | `scripts/apps-script.mjs` | (없음) | implemented |

Status 값: `draft` (사양만 있음) / `implemented` / `verified` (실제 실행까지 확인) /
`deprecated`.

테스트 연결은 해당 요구사항의 일부 검증 근거이며 전체 기능의 운영 검증을 뜻하지 않는다.
이번 문서 도입에서는 실제 Sheets 권한, 트리거, 메일 도착을 검증하지 않아 상태를 모두
`implemented`로 둔다. 운영 검증은 별도 승인 범위에서 수행한다.

## 13. 미확정 사항

- **REQ-SETTLE-001 산출 기준의 소유자 확인.** 2026-09-19에 코드를 근거로 사양으로 확정했다
  (군 9개 행 매핑, `3/8` 행축 기준, 유입 경로 `관계`·`설문지`, 일반·행축 목표 열). 실제 운영
  기준이 다르면 코드가 아니라 이 사양을 먼저 고친다.
- **상반기 방문자 동기화 재개 시점.** 하반기 시트를 준비한 뒤 다시 켜기로 했고 시점은 정해지지
  않았다. 지금은 의도적으로 비활성이다.

## 14. 향후 개선 후보

- 상반기 방문 새가족 동기화 재개. 현재 해당 함수는 의도적으로 비활성이며, 하반기 시트를
  준비한 뒤 다시 켜기로 되어 있다.
- 정착률 월간 메일 자동화. 분석은 끝났으나 Apps Script 밖의 실행 기반이 필요해 범위에서
  제외했다(`docs/settlement-monthly-email-analysis.md`).
- 교육 증분 반영(REQ-EDU-001)과 매칭·검토 분류(REQ-EDU-002)에 대한 단위 테스트. 현재 이
  프로젝트에서 가장 복잡한 판단 로직인데 로컬 검증이 없다.
- 문자공지 대상자 선정 기준(REQ-NOTI-001)에 대한 단위 테스트.
