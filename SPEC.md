# 뉴액츠 새가족·교육 통합 자동화 사양서

<!-- spec-template: v1 -->

| 항목 | 값 |
|---|---|
| Document Version | 1.1.0 |
| Project Version | 1.0.0 |
| Last Updated | 2026-09-26 |
| Status | active |
| Owner | 새가족부 운영 담당자 (역할명) |

이 문서는 뉴액츠 새가족 자동화가 **어떻게 동작해야 하는가**를 정한다. 코드가 지금 그렇게 움직인다고 해서 그것이 사양이 되지는 않는다.

사양과 구현이 다르면 코드에 맞춰 이 문서를 고치지 않는다. `SPEC / CODE MISMATCH` 형식으로 알린다. 절차는 `AGENTS.md`에 있다.

## 1. 목적

새가족부 운영진이 새가족의 **등록 → 교육 출석 → 수료 → 보고**를 손으로 옮겨 적지 않고 운영하게 돕는 자동화다.

성공 조건은 두 가지다.

- 출석 응답이 들어오면 사람이 손대지 않아도 교육 명단, 등록 명단, 수료현황이 서로 맞는다.
- 주간 운영 판단에 필요한 명단과 통계가 정해진 시각에 담당자에게 메일로 도착한다.

자동으로 판단할 수 없는 건은 임의로 고치지 않고 **검토 대상으로 보고**한다.
이유: 아무도 모르게 틀린 값을 쓰는 것보다 사람이 확인하도록 남기는 쪽이 항상 옳다.

### 이 문서에서 쓰는 말

아래 말에는 `docs/SPEC.html`에서 마우스를 올리면 뜻이 보인다.

| 용어 | 뜻 |
|---|---|
| 출석 웹앱 | 교육 참석자가 이름·전화번호로 조회하고 이번 주차 출석을 내는 웹 화면이다 |
| 주차 | 교육의 몇 번째 주인지를 뜻한다. 1~4주차가 있다 |
| 출석 응답 시트 | 웹앱과 Google Form이 출석 응답을 기록하는 시트다. 최신 응답이 머리글 바로 아래 2행에 온다 |
| 교육 출석 현황 | 교육관리 스프레드시트에 있는 기준 교육 명단이다. 자동화가 갱신한다 |
| 등록 명단(`등록 새가족`) | 등록 스프레드시트에 있는 새가족 등록 명단 시트다 |
| 군 현황판(`등록 새가족 군 현황`) | 등록 명단을 군별로 모아 자동화가 다시 쓰는 시트다 |
| 수료현황 | 등록 명단과 교육 출석을 전화번호로 묶어 다시 쓰는 시트다 |
| 군 | 새가족이 속하는 단위다. 신·조·명·총·영·석·전·슬·임의 9개이고 회기마다 바뀐다 |
| 회기 | 3월에 시작하는 운영 기간이다. 군 구성이 회기마다 바뀐다 |
| 예배 구분 | 4부 또는 5부다. 기준 시각 13:30 이전은 4부, 이후는 5부다 |
| 스스로 | 소개자가 "스스로"이거나 군배정필요로 표기된 등록자다. 담당자가 직접 군을 정해 줘야 하며, 현황판에서 4부 열과 5부 열로 나뉜다 |
| 미배정 | 군을 확정하지 못한 등록자를 적는 현황판 열이다. 여기 적힌 사람은 검토 내역으로도 보고된다 |
| `행축` | 결산 구분의 하나다. `3/8` 이후 등록자 가운데 유입 경로가 `관계` 또는 `설문지`인 사람이다. 나머지는 `일반`이다 |
| 타임스탬프 | 응답이 제출된 시각이다 |
| 커서 | 지난 실행에서 본 가장 늦은 응답의 제출 시각이다. 이보다 늦은 응답만 새로 처리한다 |
| 스크립트 속성(`PropertiesService`) | Apps Script 프로젝트에 값을 저장해 두는 곳이다. 커서와 등록 보정 상태가 여기에 있다 |
| 스크립트 잠금(`LockService`) | 한 번에 한 실행만 작업하게 막는 장치다 |
| 미리보기 | 시트·속성·메일을 바꾸지 않고 결과만 보여 주는 실행이다(dry-run) |
| 검토 대상 | 자동으로 판단할 수 없어 시트를 바꾸지 않고 사람이 확인하도록 보고하는 건이다 |
| 매칭 | 응답이나 명단의 줄을 같은 사람의 줄과 짝짓는 것이다 |
| 정규화 | 비교하기 쉽게 값의 형식을 한 가지로 맞추는 것이다. 예: 전화번호를 `010-xxxx-xxxx` 형식으로 맞춘다 |
| 공용 발송 관문 | 프로젝트마다 하나 있는 메일 발송 함수다. 모든 메일이 이곳을 거치고, 테스트 수신자 고정 규칙도 여기서 적용된다 |
| 테스트 수신자(`testRecipient`) | 안전장치가 메일 수신자로 고정하는 한 사람이다 |
| `run*Test` | 테스트용 이름이 붙은 실행 함수다. 메일을 보내는 것은 테스트 수신자 안전장치를 켠다 |
| `active` | 교육·등록 설정 값이다. 거짓이면 예약 실행이 아무 작업도 하지 않는다 |
| `mode` | 교육·등록 설정 값이다. `PRODUCTION`이 운영이고, `TEST`면 테스트 수신자 한 명에게만 보낸다 |
| 숨김 로그 시트 | 각 스프레드시트에서 실행 요약을 최신순으로 적는 숨긴 시트다 |
| Apps Script 트리거 | Apps Script 화면의 트리거 메뉴에 등록한 예약 실행이다. 정해진 요일·시간대가 되거나 제출 같은 일이 생기면 함수를 실행한다 |
| 집중교육 | 일반 교육과 따로 진행하는 교육이다. 참석 결과를 교육 출석 현황에 합친다 |
| 동기화 도구(`scripts/apps-script.mjs`) | 저장소 코드와 운영 중인 Apps Script 코드를 비교하고, 가져오고, 올리고, 배포하는 도구다 |
| 매니페스트(`scripts/apps-script-projects.json`) | 네 Apps Script 프로젝트의 script ID를 모아 둔 파일이다 |
| clasp refresh token | clasp(Apps Script 명령줄 도구)가 사용자 홈에 저장한 로그인 정보다. 동기화 도구가 다시 쓴다 |
| 이스케이프 | 사용자가 넣은 글자가 HTML 코드로 해석되지 않게 특수 문자를 바꿔 넣는 것이다 |
| 마스킹 | 전화번호 일부를 가려서 보여 주는 것이다 |

## 2. 프로젝트 범위

### 포함

- 새가족 교육 출석 입력 웹앱(본인 확인, 주차 확인, 응답 시트 기록)
- Google Form 출석 응답 가운데 새로 들어온 것만 교육 출석 현황 시트에 반영
- 집중교육 참석 결과를 교육 출석 현황에 합치기
- 등록 명단의 군·팀·전화번호를 교육 출석 기준으로 자동 보정하고 군 현황판 갱신
- 교육 출석과 등록 명단을 합친 수료현황 다시 쓰기, 주간 전체·군별 보고 메일
- 토요일 문자공지 대상 명단(교육 진행 중 / 교육 미진행) 메일
- 집중교육 신청 Form 접수, 관리자 명단과 공개 명단의 즉시 동기화
- 실행 결과의 숨김 로그 기록과 메일 수신자 안전장치
- 저장소 코드와 운영 Apps Script 코드를 맞추는 동기화 도구(`scripts/apps-script.mjs`)

### 제외

- 실제 문자(SMS) 발송. 이 자동화는 **발송 대상 명단과 번호 목록까지만** 만든다.
- 정착률 계산과 월간 정착률 메일. 외부 시스템에 로그인해야 해서 Apps Script로 실행할 수 있는 범위 밖에 있다(`docs/settlement-monthly-email-analysis.md`).
- 개인 명단과 실행 결과 데이터를 저장소에 보관하는 일. 데이터는 Google Sheets에만 둔다.
- 교육·등록 예약 실행(Apps Script 트리거)을 자동으로 만들거나 바꾸는 일. 이 일정은 Apps Script 화면에서 사람이 관리한다. 단, 집중교육 신청 설치 함수가 제출 때 실행되는 트리거를 만드는 일은 포함한다.
- 운영 배포 자동화. 배포는 사람이 승인한 뒤 손으로 실행한다.

## 3. 시스템 구성

네 개의 서로 독립된 Apps Script 프로젝트로 이루어진다. 디렉터리 하나가 Apps Script 프로젝트 하나다.

같은 디렉터리의 `.gs` 파일은 전역 설정과 공통 함수를 함께 쓴다. 그래서 따로 떼어 독립 스크립트로 나누지 않는다.

| 구성 요소 | 책임 | 위치 |
|---|---|---|
| 출석 웹앱 | 교육 참석자 본인 확인, 주차 확인, 응답 시트 기록 | `attendance-webapp/` |
| 교육 관리 | 새 응답 반영, 집중교육 합치기, 문자공지 명단, 교육 메일 발송 관문 | `education-project/` |
| 등록·보고 | 등록 정보 자동 보정, 군 현황판, 수료현황, 주간 보고 메일, 결산 | `registration-project/` |
| 집중교육 신청 | Form과 관리자·공개 Spreadsheet 설치, 제출 때 즉시 동기화 | `intensive-training-application/` |
| 동기화 도구 | 저장소와 운영 코드 비교·가져오기·올리기·배포(`diff`/`pull`/`push`/`deploy`) | `scripts/apps-script.mjs` |
| 정적 검증 | Apps Script API를 부르지 않고 이 컴퓨터에서 돌리는 함수·소스 검사 | `tests/` |

이 자동화가 바깥 시스템과 만나는 곳은 Google Sheets, Google Forms, Google Drive, `MailApp`, `PropertiesService`, `LockService`다. 모두 Apps Script를 실행하는 계정의 권한으로 접근한다.

저장소 쪽에서 바깥과 만나는 곳은 Apps Script API(`scripts/apps-script.mjs`)와 clasp가 저장한 refresh token 하나다.

### 기능 연결도

출석부터 주간 보고까지의 데이터 연결은 다음과 같다. 화살표는 데이터가 전달되거나 다음 기능이
그 데이터를 읽는 방향이다. 각 상자의 세부 규칙은 5절의 요구사항에 있다.

```flow
출석 웹앱 -> 출석 응답 시트
출석 Form -> 출석 응답 시트
출석 응답 시트 -> 교육 관리 -> 교육 출석 현황
교육 관리 -> 문자공지 대상 메일
교육 출석 현황 -> 등록·보고
등록 명단 -> 등록·보고
등록·보고 -> 군 현황판
등록·보고 -> 수료현황
등록·보고 -> 주간 보고 메일
등록·보고 -> 상반기 결산
```

집중교육 신청 접수와 참석 반영은 서로 다른 기능이다. 신청 Form을 제출해 만든 공개 명단이
교육 출석 현황으로 자동 반영되는 경로는 없다.

```flow
집중교육 신청 Form -> 집중교육 신청 관리 -> 비공개 관리자 명단
집중교육 신청 관리 -> 공개 확인 명단
집중교육 참석 표시 -> 교육 관리 -> 교육 출석 현황
```

## 4. 전체 동작 흐름

1. 참석자가 출석 웹앱에서 이름·전화번호로 조회하고 이번 주차 출석을 낸다.
2. 서버가 제출 값을 다시 확인한 뒤, 출석 응답 시트의 **머리글 바로 아래(2행)** 에 끼워 넣는다.
3. 교육 프로젝트가 **화요일 17:00~18:00에 한 번** 실행된다. 커서 이후에 들어온 새 응답만 오래된 것부터 교육 출석 현황에 반영하고, 결과를 메일과 숨김 로그로 남긴다.
4. 등록 프로젝트가 월요일에 교육 출석을 기준으로 등록 명단의 군·팀·전화번호를 보정하고 군 현황판을 다시 쓴다. 사람이 직접 고친 값은 보호한다.
5. 등록 프로젝트가 금요일에 등록 명단과 교육 출석을 전화번호로 묶어 수료현황을 다시 쓴다. 그리고 전체 관리자와 군 담당자에게 주간 통계를 보낸다.
6. 교육 프로젝트가 토요일에 문자공지 대상 명단과 번호 목록을 메일로 보낸다.
7. 집중교육 신청은 위 흐름과 따로 돌아가는 독립 프로젝트다. Form이 제출되면 곧바로 관리자 명단과 공개 확인 명단을 갱신한다.

아래 그림의 요일은 각 기능의 실행 시점이다. 화살표가 월요일·화요일·금요일·토요일을
한 주 안의 실행 순서로 나타내는 것은 아니다.

```flow
출석 제출 -> 출석 응답 시트
출석 응답 시트 -(화요일)-> 교육 출석 갱신 -> 교육 출석 현황
교육 출석 현황 -(월요일)-> 등록 보정·군 현황판
등록 명단 -(금요일)-> 수료현황·주간 통계
교육 출석 현황 -(금요일)-> 수료현황·주간 통계
교육 출석 현황 -(토요일)-> 문자공지 대상 메일
```

정기 실행과 별도로 담당자가 실행하거나 신청자가 제출하면 바로 처리되는 경로도 있다.

```flow
집중교육 참석 표시 -(메뉴 실행)-> 교육 출석 현황
집중교육 신청 Form -(제출 즉시)-> 비공개 관리자 명단
집중교육 신청 Form -(제출 즉시)-> 공개 확인 명단
등록 명단 -(수동 실행)-> 상반기 결산
```

## 5. 기능 요구사항

ID 규칙: `REQ-<CATEGORY>-NNN`. CATEGORY는 대문자·숫자, NNN은 세 자리다.
한 번 붙인 ID는 다시 쓰거나 뜻을 바꾸지 않는다. 지운 ID를 다른 기능에 돌려쓰지 않는다.

아래 기능 그룹 표는 CATEGORY마다 사람이 읽는 이름을 붙인다. `docs/SPEC.html`의 기능 목록이 이 이름으로 묶인다.

| 카테고리 | 이름 |
|---|---|
| ATTEND | 출석 웹앱 |
| EDU | 교육 출석 |
| NOTI | 문자공지 |
| REG | 등록·군 현황 |
| REPORT | 보고 |
| SETTLE | 결산 |
| INTENSIVE | 집중교육 |
| MAIL | 메일 안전장치 |
| LOG | 실행 로그 |
| SEC | 보안 |
| OPS | 운영 |
| DATA | 개인정보 |
| PERF | 성능 |
| SYNC | 배포 동기화 |

### REQ-ATTEND-001 출석 제출 확인

#### 목적
잘못된 입력, 같은 출석을 두 번 낸 제출, 조작된 제출이 시트에 남지 않게 한다.

#### 동작
- 조회할 때는 이름과 휴대폰 번호가 **모두** 맞는 기존 응답을 찾아, 그 가운데 가장 높은 주차를 알려 준다.
- 제출 값은 화면 쪽에서 한 확인을 믿지 않고 서버에서 다시 확인한다. 확인하는 값은 다음과 같다.
  - 이름: 30자 이하
  - 휴대폰 번호: 10~11자리
  - 주차: 1~4
  - 군: 허용 목록에 있는 값
- 1주차 제출에는 유입 경로·성별·나이가 모두 있어야 한다.
- 2주차 이상은 기존 기록에서 성별·나이를 가져온다. 기존 기록이 없으면 받지 않는다.
- 받을 수 있는 주차는 **기존 최고 주차 + 1** 하나뿐이다.
- 이미 기록된 주차를 다시 내면 새 줄을 만들지 않고 중복이라고 응답한다.
- 제출 처리 전체를 스크립트 잠금으로 감싼다.
  이유: 연속 클릭이나 동시 요청이 같은 줄을 두 번 만들지 못하게 한다.
- 화면에 사용자 이름을 넣을 때 HTML을 이스케이프한다.

#### 예외 처리
확인 실패, 잠금을 얻지 못한 경우, 기존 기록과 맞지 않는 경우에는 모두 예외를 내고 멈춘다. 이때 시트는 바꾸지 않는다.
사용자에게는 원인을 알 수 있는 메시지를 보여 준다.

#### 관련 구현
`attendance-webapp/Code.gs`, `attendance-webapp/Index.html`

#### 관련 테스트
TEST-ATTEND-001

### REQ-ATTEND-002 응답 시트를 최신순으로 유지

#### 목적
운영자가 보는 응답 시트를 늘 최신순으로 유지한다.

#### 동작
- 새 응답은 시트 끝에 붙이지 않고 **머리글 바로 아래 2행**에 끼워 넣는다.
- 끼워 넣은 줄의 타임스탬프 열에는 시트의 다른 줄과 같은 날짜 형식을 적용한다.
- 과거 응답을 한 번에 최신순으로 정렬해야 할 때는 `sortAttendanceSourceNewestFirst`를 손으로 1회 실행한다. 정기 실행은 시트 전체를 다시 정렬하지 않는다.
- 끼워 넣는 위치가 바뀌어도 교육 반영에서 빠지는 응답이 없어야 한다(REQ-EDU-001).
  이유: 교육 반영은 행 번호 대신 타임스탬프를 기준으로 삼는다.

#### 예외 처리
끼워 넣기에 실패하면 예외를 그대로 올리고 잠금을 푼다.

> **주의** 줄 끼워 넣기, 값 쓰기, 서식 지정은 따로따로 하는 호출이다. 중간에 실패하면 일부만 기록된 상태가 남을 수 있고, 이를 자동으로 원래대로 되돌린다는 약속은 없다.

#### 관련 구현
`attendance-webapp/Code.gs`

#### 관련 테스트
TEST-ATTEND-002

### REQ-EDU-001 교육 출석 현황 갱신

#### 목적
같은 응답을 두 번 반영하거나 새 응답을 빠뜨리지 않고 교육 출석 현황을 갱신한다.

#### 동작
- 처리 기준은 **타임스탬프 커서**다. 커서보다 늦은 응답만 처리한다.
- 응답 시트가 최신순으로 쌓여 있어도 처리는 타임스탬프가 이른 것부터 한다. 1주차부터 차례로 반영한다.
- 커서는 미리보기(dry-run)가 아닌 실제 실행에서만 옮긴다. 옮기는 값은 이번 실행에서 본 가장 늦은 타임스탬프다.
- 예전 방식의 행 번호 커서가 남아 있으면, 그 줄의 타임스탬프로 바꿔 이어받은 뒤 행 번호 커서는 지운다.
- 커서가 없고 행 번호로 바꿀 수도 없으면 아무것도 처리하지 않고 초기화가 필요하다고 보고한다.
- 미리보기 함수는 시트·속성·메일을 전혀 바꾸지 않고 처리할 예정 건수만 알려 준다.

#### 예외 처리
원본 시트를 찾지 못하면 예외를 내고 멈춘다. 새 응답이 0건이면 정상으로 끝내고 메일을 보내지 않는다.

#### 관련 구현
`education-project/교육 출석 현황 업데이트.gs`

#### 관련 테스트
(없음. 로컬 정적 테스트가 다루지 않는다)

### REQ-EDU-002 출석 대상자 매칭 보호

#### 목적
사람을 잘못 짝지어 남의 출석을 기록하거나 기존 값을 덮어쓰는 일을 막는다.

#### 동작
- 자동으로 매칭하는 경우는 **정규화한 전화번호가 교육 시트에서 정확히 한 줄과 같을 때**뿐이다.
- 다음 경우는 반영하지 않고 검토 대상으로 분류한다.
  - 같은 전화번호가 여러 줄에 있을 때
  - 2주차 이상인데 전화번호로 찾지 못했을 때. 이름이 같아도 자동 반영하지 않는다.
  - 필수값이 빠졌거나 허용되지 않은 주차일 때
- 새 사람 추가는 1주차 응답일 때만 한다.
- 일요일이 아닌 때 제출된 1주차 응답은 자동 추가하지 않고 검토 대상으로 둔다.
  이유: 예배 구분을 알 수 없다.
- 해당 주차 칸에 이미 값이 있으면 덮어쓰지 않고 중복으로 분류한다.
- 군·팀은 가장 최근 응답 값으로 바꾸고, 바뀐 내용을 정보변경으로 기록한다.
- 실행 결과는 다섯 가지로 나눠 메일로 보고한다: 신규 추가 / 출석 반영 / 중복 / 정보변경 / 검토 필요.

#### 예외 처리
검토·중복은 실패로 보지 않는다. 정상적인 부분 성공이다.
시트는 판단할 수 있는 줄만 바꾸고, 나머지는 원본을 그대로 둔 채 보고한다.

#### 관련 구현
`education-project/교육 출석 현황 업데이트.gs`

#### 관련 테스트
(없음. 로컬 정적 테스트가 다루지 않는다)

### REQ-EDU-003 집중교육 참석 반영

#### 목적
집중교육 참석자를 일반 교육 출석 현황에 반영한다. 이때 기존 기록은 망가뜨리지 않는다.

#### 동작
- 집중교육 시트에서 참석 표시가 있는 줄만 다룬다.
- 참석 표시는 숫자 `0`과 영문 `o`·`O`를 같은 뜻으로 읽는다.
- 분기 번호가 날짜 형식으로 저장된 줄도 화면에 보이는 `3-1` 같은 번호로 분기를 읽는다.
- 전화번호로 교육 출석 현황의 줄을 찾는다. 여러 줄이 걸리면 반영하지 않고 충돌로 기록한다.
- 맞는 줄이 없으면 1~3주차를 완료로 표시한 새 줄을 더한다.
- 맞는 줄이 있으면 **비어 있는 주차 칸만 채운다.** 이미 값이 있는 칸은 그대로 둔다.
- 4주차 칸에 다른 값이 이미 있으면 그 값을 남기고 충돌로 기록한다.
- 이전 실행이 분기 번호를 날짜로 잘못 읽어 4주차에 적은 표기는 원본 번호와 날짜가
  일치할 때에만 올바른 분기 이름으로 고친다.
- 이 기능은 예약 실행으로 돌리지 않는다. 교육관리 시트 상단의 `집중교육` 메뉴에서
  `참석 명단 반영`을 누르면 실행 결과를 알림창으로 보여 준다.
- Apps Script 편집기에서 실행할 때는 먼저 미리보기 함수로 확인한다.

> **주의** `previewIntensiveTraining`은 시트를 바꾸지 않는다. 승인한 뒤 실행하는 `applyIntensiveTrainingNow`는 실제 시트를 바꾼다. 이 함수는 메일을 보내지 않고 `run*Test` 계열도 아니므로, 이름만 보고 미리보기라고 판단하지 않는다.

#### 예외 처리
집중교육 시트를 찾지 못하면 예외를 내고 멈춘다. 메뉴에서 실행한 경우에는 실패 원인을 알림창으로 보여 준다. 충돌은 실패로 보지 않고 검토 항목으로 보고한다.

#### 관련 구현
`education-project/집중교육 출석 현황 업데이트.gs`

#### 관련 테스트
TEST-EDU-003

### REQ-NOTI-001 주말 문자공지 대상 뽑기

#### 목적
주말 문자공지 담당자가 대상자와 번호를 손으로 추리지 않게 한다.

#### 동작
- 명단을 두 가지 만든다.
  - **교육 진행 중**: 교육 출석 현황에 있고 4주차를 마치지 않은 사람
  - **교육 미진행**: 수료현황에 등록됐지만 1주차를 시작하지 않은 사람
- 날짜를 읽을 수 있는 기록은 "최근 15주"와 "고정 시작일" 가운데 늦은 날짜부터 본다.
- 날짜를 읽지 못한 줄은 다른 제외 조건에 걸리지 않으면 명단에 들어갈 수 있다.
- 문자 제외 표시가 있는 사람은 두 명단 모두에서 뺀다.
- 이름이나 전화번호가 없는 줄은 뺀다.
- 문자 발송용 번호 목록은 다음처럼 만든다.
  - 같은 전화번호는 한 번만 넣는다.
  - 임원 번호를 넣는다.
  - 복사하기 쉽게 20명마다 구분선을 넣는다.
- 메일은 교육 프로젝트의 공용 발송 관문으로 보낸다. 이 파일이 운영 수신자 목록을 따로 가지고 있으면 안 된다(REQ-MAIL-001).

#### 예외 처리
수료현황 시트를 찾지 못하면 예외를 내고 멈춘다.

#### 관련 구현
`education-project/문자 명단 리스트.gs`

#### 관련 테스트
(없음. 대상자를 고르는 기준을 확인하는 테스트가 없다)

### REQ-REG-001 등록 명단 자동 보정

#### 목적
등록 명단의 군·팀·전화번호를 교육 출석의 최신 값에 맞춘다. 단, 사람이 손으로 고친 값을 자동화가 되돌리지 않게 한다.

#### 동작
- 등록 명단의 각 줄을 교육 출석 원본 응답과 매칭한다. 판단 순서는 ① 저장된 매칭 이름 ② 현재 이름과 전화번호 ③ 등록 명단에서 하나뿐인 이름이다. 최신 응답을 앞세운다.
- 자동으로 보정하는 항목은 군·팀·전화번호다.
- **이름은 자동으로 보정하지 않는다.** 등록 명단의 이름과 교육 출석의 이름이 다르면 검토 내역으로 보고하고, 어느 쪽이 맞는지는 사람이 확인한다.
- 값이 다르면 교육 쪽 최신 값으로 보정한다. 보정한 값과 바로 전 값은 스크립트 속성에 기록한다.
- 다음 실행에서 현재 값이 **바로 전에 자동 보정한 값과 다르면** 사람이 고친 것으로 본다. 그 항목은 보호 대상으로 바꾸고, 이후 자동으로 고치지 않는다.
- 이미 보호 대상인 항목은 값이 달라도 건드리지 않고 보호 내역으로만 보고한다.
- 결과 메일에는 다음을 담는다.
  - 건수: 등록자, 매칭, 미매칭, 자동수정, 수동수정 보호, 검토
  - 자동수정 상세: 행 / 이름 / 항목 / 기존값→교육 최신값 / 판단 근거
- 결과 메일은 **정기 실행 때도 테스트 수신자 한 명**에게만 간다. 관리자 전체에게 보내도록 바꾸려면 그 자체를 사양 변경으로 다룬다.
- 미리보기와 테스트 실행은 시트를 바꾸지 않는다.

#### 예외 처리
매칭되지 않는 등록자는 오류로 보지 않고 미매칭으로 세어 보고한다.

#### 관련 구현
`registration-project/등록새가족-군현황 자동 배치.gs`

#### 관련 테스트
TEST-REG-002

### REQ-REG-002 군 현황판 다시 만들기

#### 목적
군 현황판을 등록 명단에서 다시 만든다. 연도가 없는 날짜 표기도 해가 바뀌어도 맞게 읽는다.

#### 동작
- 등록 명단을 읽어 **군별로만** 세어 군 현황판 시트를 다시 쓴다.
- 예배 구분(4부/5부)으로 군 배정을 나누지 않는다. 같은 군이면 4부 등록자와 5부 등록자가 한 열에 함께 쌓인다.
- 현황판 열 구성은 `날짜 / 신·조·명·총·영·석·전·슬·임 / 스스로(4부)·스스로(5부) / 미배정 /
  합계`의 14열이다. 행 구성은 `1~2행 머리글 / 3행 합계 / 4행부터 명단`이다. 모두 자동화가 다시 쓴다.
- **명단을 먼저 쓰고 머리글을 나중에 쓴다.**
  이유: 머리글의 병합·서식은 보기 위한 것이고 명단이 본체다. 머리글 쪽 실패가 명단 기록을 막으면 안 된다.
- **머리글에 세로 병합을 쓰지 않는다.**
  이유: 시트에 필터가 걸려 있으면 1행이 필터 머리글이 되어, Google Sheets가 세로 병합을 거부한다.
- 세로 병합 대신 두 행의 역할을 나눈다.
  - 1행: 여러 열을 묶는 이름(`군 배정 (4·5부 통합)`, `스스로`)만 쓴다.
  - 2행: 모든 열의 이름을 쓴다.
- `날짜`·`미배정`·`합계`처럼 한 열짜리 제목은 2행에만 적는다.
  이유: 1행에도 적으면, 세로 병합이 안 되는 시트에서 같은 글자가 두 줄로 겹쳐 보인다.
- 가로 병합(군 묶음, 스스로 묶음)은 시도하되, 거부되면 건너뛴다.
- **"스스로"만 예배 구분에 따라 4부 열과 5부 열로 나눈다.** 눈으로 구분해야 하는 값은 이것뿐이다.
- 다음 셋 중 하나면 "스스로"로 본다.
  - 군 값이 `스스로`다.
  - 군 값이 `군배정필요`다.
  - 군 값이 비어 있고 소개자가 `스스로`다.
- 군 값은 첫 글자로 정규화해 9개 군에 맞춘다. 예를 들어 "신군"과 "신1팀"은 신이 된다. "모르겠"이 들어간 값은 군으로 인정하지 않는다.
- **현황판에서 빠지는 등록자가 없어야 한다.** 다음 사람은 `미배정` 열에 이름을 쓰고, 검토 내역으로도 함께 보고한다.
  - 군을 확정하지 못한 사람
  - "스스로"인데 예배 구분이 4도 5도 아닌 사람
- 예배 구분이 4·5가 아니어도 군을 알 수 있으면 그 군 열에 그대로 넣는다.
- `M/d` 형태의 등록일에는 연도가 없다. 그래서 **회기(3월 시작)를 기준으로 오늘 날짜에서 연도를 짐작**한다.
  - 12월 값은 오늘이 12월이 아니면 전년도로 본다.
  - 연도를 코드에 고정해 두지 않는다.

#### 예외 처리
- 등록 시트나 현황판 시트를 찾지 못하면 예외를 내고 멈춘다.
- 이름은 있지만 등록일을 읽을 수 없는 줄은 놓을 날짜가 없다. 그래서 현황판에 쓰지 않고 검토 내역으로만 보고한다.
- 머리글의 병합 실패는 실행 로그에만 남기고 멈추지 않는다.
  이유: 병합은 꾸미기일 뿐이다.
- 그 밖의 머리글 실패는 감추지 않고 예외로 올린다. 다만 그때는 명단이 이미 기록돼 있어야 한다.

#### 관련 구현
`registration-project/등록새가족-군현황 자동 배치.gs`

#### 관련 테스트
TEST-REG-001, TEST-REG-003, TEST-REG-005

### REQ-REG-003 군을 정해 줄 사람 주간 메일

#### 목적
담당자가 **직접 군을 정해 줘야 하는 사람**을 매주 메일로 받게 한다. 현황판을 열어 보지 않아도 누가 남아 있는지 알 수 있다.

#### 동작
- 군 현황판을 갱신할 때 다음 두 부류를 한 명단으로 모은다.
  - `스스로`: 스스로 열(4부 또는 5부)에 놓인 사람
  - `미배정`: 미배정 열에 놓인 사람, 그리고 등록일을 읽지 못해 현황판에 놓지 못한 사람
- 명단의 각 항목에는 구분, 등록일, 예배 구분, 등록 시트 행 번호, 이름, 군 표기 원문, 소개자를 담는다.
  이유: 받는 사람이 등록 시트에서 그 줄을 바로 찾아 고칠 수 있게 행 번호를 넣는다.
- 이 명단은 등록 유지보수 결과 메일에 **따로 떨어진 한 부분**으로 넣는다. 텍스트 본문과 HTML 본문 모두에 들어간다.
- 검토 필요 상세에는 섞지 않는다.
  이유: 스스로 등록은 오류가 아니고 처리를 기다리는 건이다.
- 메일 제목에 명단 인원 수를 넣는다. 메일을 열지 않아도 건수를 알 수 있게 하기 위해서다.
- 수신자는 REQ-REG-001과 같다. 따로 수신자 목록을 만들지 않는다.
- 명단이 비면 그 부분을 만들지 않는다.

#### 예외 처리
명단 모으기는 현황판 갱신에 딸린 결과라서 따로 실패하는 경로가 없다. 현황판 갱신이 멈추면 이 명단도 만들어지지 않는다.

#### 관련 구현
`registration-project/등록새가족-군현황 자동 배치.gs`

#### 관련 테스트
TEST-REG-004

### REQ-REG-004 현황판 인원 세기와 대조

#### 목적
현황판만 보고도 군별·전체 인원을 알 수 있게 한다.
또 **등록 명단의 인원과 현황판에 적힌 인원이 같은지 자동으로 맞춰 봐서**, 사람이 빠진 것을 놓치지 않는다.

#### 동작
- 머리글 바로 아래 **3행을 합계 행**으로 쓴다.
  - A열에 `합계`라고 적는다.
  - 각 군 열, 스스로 4부·5부 열, 미배정 열에는 그 열의 인원 수를 적는다.
  - 합계 열에는 전체 인원을 적는다.
  - 명단은 4행부터다.
- 합계는 **세어 둔 값을 다시 쓰지 않고, 출력된 칸을 직접 세어** 만든다.
  이유: 같은 수를 두 곳에 적는 것은 대조가 아니다.
- 인원 대조는 **서로 다른 세 경로로 얻은 수**를 맞춰 본다.
  1. `등록 명단에서 이름이 있는 행 수`
  2. `배치 단계에서 센 수`
  3. `출력된 칸을 다시 세어 얻은 수`. 여기에 `주별 합계 열을 더한 값`까지 네 번째로 본다.
- 네 값이 모두 같으면 일치, 하나라도 다르면 불일치다.
- 결과 문장에는 **맞을 때도 근거가 된 인원 수를 함께 적는다.**
  이유: "이상 없음"만으로는 검사가 무엇을 봤는지 알 수 없다.
- 불일치면 다음을 한다.
  - 검토 내역에 항목을 더한다.
  - 배치하지 못한 사람의 이름과 등록 시트 행 번호를 결과 문장에 넣는다.
  - 메일 제목에도 불일치 표시를 넣는다.
- 불일치여도 시트 기록을 멈추지 않는다. 기록하고 보고한다.

#### 예외 처리
대조는 보고용이라 실패하는 경로가 없다. 현황판 갱신이 멈추면 대조도 하지 않는다.

#### 관련 구현
`registration-project/등록새가족-군현황 자동 배치.gs`

#### 관련 테스트
TEST-REG-006

### REQ-REPORT-001 수료현황·주간 통계 보고

#### 목적
등록 명단과 교육 출석을 합친 수료현황과 주간 통계를 정해진 시각에 담당자에게 보낸다.

#### 동작
- 등록 명단과 교육 출석 현황을 **전화번호로** 묶어 수료현황 시트를 다시 쓴다.
- 이 과정에서 등록 시트의 군·팀·전화번호는 자동으로 덮어쓰지 않는다. 서로 다르면 보고만 한다.
- 전체 관리자에게는 전체 통계 메일을, 군 담당자에게는 그 군의 통계 메일을 보낸다.
- 미리보기와 테스트 실행은 시트를 바꾸지 않는다. 테스트 실행은 메일을 한 통만 보낸다.
- 실행 결과는 수료 자동화 로그에 기록한다(REQ-LOG-001).

#### 예외 처리
필요한 시트를 찾지 못하면 예외를 내고 멈춘다. 미매칭, 중복, 불일치는 세어서 보고한다.

#### 관련 구현
`registration-project/등록 새가족 새가족교육 수료현황 자동화.gs`

#### 관련 테스트
(없음. 로컬 정적 테스트가 다루지 않는다)

### REQ-SETTLE-001 상반기 새가족 결산

#### 목적
상반기에 등록한 새가족 현황을 군, 날짜, 일반/행축 구분으로 센다. 그리고 목표 대비 실적을 한 화면에 보여 준다.

#### 동작
- 원본은 등록 명단이고 출력은 `상반기 결산` 시트다. 둘 중 하나라도 없으면 멈춘다.
- 결산 시트 1행의 날짜 표기와 2행의 구분(`일반`/`행축`)을 읽어, `날짜_구분`마다 몇 번째 열인지 짝을 만든다.
- 1행이 빈 칸이면 병합된 칸으로 보고 바로 앞 날짜를 이어 쓴다.
- 군은 등록 명단 군 값의 첫 글자로 정한다. 9개 군(신·조·명·총·영·석·전·슬·임)을 3~11행에 대응시킨다.
- 등록일은 회기를 기준으로 연도를 짐작한 뒤 `M/d` 표기로 맞춘다.
- 날짜가 `3/8` 이전이거나 12월~2월이면 `행축` 판정 대상이 아니다.
- 유입 경로가 `관계` 또는 `설문지`이고 `3/8` 이후면 `행축`, 그 밖은 `일반`으로 센다.
- 다시 계산하기 전에 군 행의 계산 영역(일반·행축 합계와 비율, 날짜별 열)과 12행 합계를 지운다. **목표값 열은 건드리지 않는다.**
- 12행에 군별 합계와 전체 목표 대비 비율을 쓴다. 비율 칸에는 백분율 서식을 적용한다.
- 군이나 날짜/구분 열을 찾지 못한 줄은 세지 않고 검토 내역으로 보고한다.
- `previewSettlementReport`는 시트를 바꾸지 않는다. 시트에 반영하는 함수는 `generateSettlementReport`다. 운영 모드에서 등록 자동화가 꺼져 있으면 이 함수는 멈춘다.
- 예약 실행은 없다. 필요할 때 손으로 실행한다.

#### 예외 처리
필요한 시트를 찾지 못하면 예외를 내고 멈춘다. 검토 내역은 실패로 보지 않는다. 집계에서 뺀 항목이다.

#### 관련 구현
`registration-project/제목 없음.gs`

#### 관련 테스트
TEST-SETTLE-001

### REQ-INTENSIVE-001 집중교육 신청 접수

#### 목적
집중교육 신청을 Form 한 번 제출로 받는다. 전화번호는 공개하지 않으면서, 신청 현황은 누구나 볼 수 있게 한다.

#### 동작
- 군은 9개다. 군을 고르면 그 군의 팀 Section(Form의 질문 묶음 화면)으로만 넘어간다. 그래서 있을 수 없는 군·팀 조합이 만들어지지 않는다.
- Form 표기(`명군`)와 시트 저장 표기(`명`)를 서로 바꾼다. 정렬할 때는 원래 표기로 되돌린다.
- 전화번호는 숫자만 넣어도 `010-xxxx-xxxx` 형식으로 정규화한다. 11자리만 받는다.
- 제출되면 관리자 명단에는 오류 상태까지 함께 기록한다. 공개 명단에는 필수값·군·팀·전화번호가 올바른 응답만 더한다.
- 공개 명단은 군 → 팀 → 참석자 이름 순으로 정렬한다.
- 참석자 이름과 전화번호가 모두 같은 이전 신청이 있으면 관리자 명단에 중복 의심으로 표시한다.
- 트리거는 Form 대신 **관리자 Spreadsheet의 제출 이벤트**에 건다.
- 처음 설치하는 함수는 폴더, Form, Spreadsheet 2개를 만들고, 전체 분기 구성, 공유 권한, 트리거까지 직접 확인한다.
- 설치 중 실패하면 속성과 그 제출 트리거를 정리하고, 만든 파일·폴더를 휴지통으로 옮기려고 시도한다.

> **주의** 정리에 실패하면 로그에만 남는다. 그래서 설치 전 상태로 완전히 되돌아간다는 약속은 없다.

#### 예외 처리
- 설치 확인에 실패하면 정리를 시도한 뒤 멈춘다.
- 이미 설치 속성이 있으면 다시 만들지 않는다.
- 다시 설치하려면 속성과 트리거를 초기화한 뒤 진행한다. 기존 Drive 파일은 따로 관리한다.

#### 관련 구현
`intensive-training-application/Code.gs`

#### 관련 테스트
TEST-INTENSIVE-001

### REQ-MAIL-001 테스트 메일 오발송 방지

#### 목적
테스트로 돌린 실행이 운영 수신자 전체에게 메일을 보내는 사고를 구조로 막는다.

#### 동작
- 모든 메일은 프로젝트마다 하나 있는 공용 발송 관문을 거친다. 개별 기능이 `MailApp`을 직접 부르지 않는다.
- `forceTestRecipient`가 켜져 있거나 모드가 운영이 아니면, 수신자를 **테스트 수신자 한 명으로 고정**하고 제목 앞에 `[TEST]`를 붙인다.
- 호출하는 쪽이 운영 수신자 목록을 직접 넘겨도 위 규칙이 이긴다.
- 테스트 강제 상태에서 최종 수신자가 정확히 그 한 명이 아니면 보내지 않고 예외를 내고 멈춘다.
- 수신자 목록은 공백을 지운 뒤 중복을 없앤다.
- 운영 수신자 목록은 프로젝트마다 설정 객체 한 곳에서만 관리한다. 다른 파일이 따로 목록을 가지지 않는다.
- 메일 아래쪽의 수신자 안내 문구는 받는 범위와 맞아야 한다. 운영 발송에 "한 명에게만 발송" 같은 문구를 쓰지 않는다.
- 메일을 보내는 교육 테스트, 문자공지 테스트, 등록 유지관리 테스트, 수료보고 테스트는 이 안전장치를 켠다.

> **참고** 집중교육 반영은 `applyIntensiveTrainingNow`가 메일 없이 실제 시트를 바꾼다.

#### 예외 처리
안전장치를 어기는 경우는 발송 실패로 다루지 않고, 실행을 멈추는 사유로 다룬다.

#### 관련 구현
`education-project/교육 출석 현황 업데이트.gs`,
`registration-project/등록새가족-군현황 자동 배치.gs`

#### 관련 테스트
TEST-MAIL-001, TEST-MAIL-002

### REQ-LOG-001 실행 로그 기록

#### 목적
실행 결과를 나중에 따라가 볼 수 있게 남긴다. 다만 운영 화면을 어지럽히지 않는다.

#### 동작
- 교육 새 응답 처리, 등록 유지관리, 수료보고가 기록할 때는 요약을 로그 시트 2행에 최신순으로 쓴다.
- 교육 새 응답이 없거나 커서 초기화가 필요하면 기록하기 전에 끝낸다.
- 다른 보이는 시트가 있으면 새 로그 시트와 기존에 보이던 로그 시트를 숨긴다.
- 로그는 미리보기(dry-run)가 아닌 실제 실행에서만 기록한다.
- 로그 기록 함수를 아무 일도 하지 않는 빈 함수(stub)로 두지 않는다. 기록을 끄는 것은 사양 변경이다.

> **주의** 로그 시트를 숨길 때 첫 번째로 보이는 운영 시트를 활성화한다. 그래서 사용자가 보던 탭이 그대로 남는다는 약속은 없다.

#### 예외 처리
로그 기록에 실패하면 예외를 그대로 올린다. 이미 한 시트 변경은 되돌리지 않는다.

#### 관련 구현
`education-project/교육 출석 현황 업데이트.gs`,
`registration-project/등록새가족-군현황 자동 배치.gs`,
`registration-project/등록 새가족 새가족교육 수료현황 자동화.gs`

#### 관련 테스트
TEST-LOG-001

## 6. 비기능 요구사항

### NFR-SEC-001 개인정보·인증 정보는 저장소 밖에 보관

개인정보와 인증 정보는 저장소 밖에 둔다.

- 개인 명단, 실행 결과, 시트 데이터, clasp 인증 파일을 저장소·Knowledge·Evidence·커밋 메시지에 넣지 않는다.
- `.gitignore`는 clasp 파일, 환경변수 파일, 일부 도구 산출물을 제외한다. 아무 개인정보 파일이나 자동으로 막아 주지는 않으므로, 커밋할 파일을 따로 확인한다.
- 집중교육 관리자 파일과 시스템 폴더는 소유자만 볼 수 있다. 공개 확인 시트만 링크가 있으면 볼 수 있는 링크 뷰어 권한을 갖는다.
- 공개 확인 시트의 열은 번호, 군, 팀, 참석자 이름, 신청자 이름뿐이다. 전화번호, 이메일, 계정, 응답 ID는 넣지 않는다.
- 교육 결과 상세의 전화번호는 마스킹한다.
- 문자공지, 등록 보정 상세처럼 전체 번호가 들어가는 보고도 있다. 그래서 메일 받는 범위를 공용 발송 관문으로 제한한다.

#### 관련 테스트
TEST-SEC-001

### NFR-OPS-001 겹치지 않는 정기 실행

정기 실행은 예측할 수 있어야 하고, 겹쳐 실행되지 않아야 하며, 한 줄로 멈출 수 있어야 한다.

- 설치형 트리거(Apps Script 트리거 메뉴에 등록한 예약 실행)는 매주 정해진 일정으로 돈다.
- Apps Script가 정한 시간대 안에서 실행 시각을 고른다. 그래서 정각에 실행되지 않을 수 있다.
- 운영 일정은 아래 표와 같다.

| 작업 | 요일 | 시간대 |
|---|---|---|
| 교육 출석 반영 | 화요일 | **17:00~18:00 한 번** |
| 등록 유지관리 | 월요일 | 08:00~09:00 |
| 등록 수료·주간 통계 | 금요일 | 11:00~12:00 |
| 문자공지 | 토요일 | 08:00~09:00 |

- 교육 출석 반영은 하루 한 번이면 충분하다. 그래서 금요일 실행을 두지 않는다.
- 교육·등록 예약 실행은 스크립트 잠금 안에서 돈다. 30초 안에 잠금을 얻지 못하면 작업하지 않고 멈춘다.
- 출석 웹앱 제출은 잠금을 10초 기다린다.
- 설정의 `active`가 거짓이면, 예약 실행으로 불린 함수는 어떤 데이터도 바꾸지 않고 바로 끝난다.
- 함수 이름을 바꾸면 트리거 연결이 끊어진다. 그래서 기존 트리거 함수 이름은 호환용 래퍼(예전 이름으로 불려 새 함수를 대신 부르는 함수)로 남겨 둔다.

#### 관련 테스트
(없음)

### NFR-DATA-001 개인정보 자동 삭제 금지

개인정보를 담은 원본, 로그, 보정 상태는 자동으로 지우지 않는다.

- 출석 응답 원본, 숨김 로그 시트, 등록 보정 상태 속성을 얼마나 보관할지는 지금 정하지 않는다.
- 자동으로 지우거나 기한이 지나면 없애는 코드를 두지 않는다.
- 정리가 필요해지면 누가 지울지, 얼마마다 지울지, 백업할지를 이 사양에 먼저 적고 그다음 구현한다.
- 저장소 밖 시트를 임의로 지우지 않는다.
- 보정 상태 속성에는 개인정보 값과 수동 수정 보호 정보가 들어 있다. 그래서 정리할 때는 보호 상태가 사라지지 않게 할 방법을 함께 정한다.

#### 관련 테스트
TEST-DATA-001. 보존 **기간** 정책은 코드로 검증하지 않는다. 다만 자동 삭제·만료 코드를 두지 않는다는 금지는 검증한다.
이유: 검증하지 않으면 누군가 정리 코드를 넣어도 드러나지 않는다.

### NFR-PERF-001 Apps Script 한도 안에서 실행

Apps Script의 실행 시간과 호출 한도 안에서 동작해야 한다.

- 교육 출석, 군 현황판, 수료현황은 배열로 한 번에 쓴다. 등록 자동 보정은 바뀐 칸만 하나씩 기록한다.
- 스크립트 속성은 등록자 수만큼 하나씩 읽고 쓰지 않는다. 한 번에 읽고 한 번에 저장한다.
- 응답 시트 전체를 읽되, 커서 이후 응답만 반영한다(REQ-EDU-001).

#### 관련 테스트
TEST-PERF-001

### NFR-SYNC-001 운영 Apps Script 동기화

저장소 코드가 운영 중인 Apps Script 코드와 같아야 한다. 다르면 그 차이가 드러나야 한다.

- 네 프로젝트의 script ID는 매니페스트 한 곳(`scripts/apps-script-projects.json`)에서 관리한다.
- `diff`로 운영 파일과 로컬 파일이 같은지, 다른지, 한쪽에만 있는지 확인한다.
- `push`는 그 프로젝트의 파일 전체를 바꾼 뒤 **다시 읽어서 같은지 확인**한다. 여전히 다르면 실패로 보고한다.
- 웹앱은 버전을 고정해 배포한다. 그래서 코드를 올리는 것만으로는 사용자 화면이 바뀌지 않는다. 새 버전을 만들어 기존 배포에 연결해야 한다.
- 인증은 clasp가 저장한 refresh token을 다시 쓴다. 토큰 파일은 저장소에 두지 않는다.

#### 관련 테스트
(없음)

## 7. 데이터 사양

이 자동화가 다루는 개인 데이터는 **성명, 휴대폰 번호, 성별, 나이, 소속 군·팀, 등록일, 교육 출석일, 유입 경로**다. 실제 값은 이 문서와 저장소 어디에도 쓰지 않는다.

| 데이터 | 위치 | 성격 |
|---|---|---|
| 교육 출석 원본 응답 | 출석 응답 스프레드시트 | 웹앱·Form이 새 줄을 더한다. 최신순 정렬 |
| 교육 출석 현황 | 교육관리 스프레드시트 | 자동화가 갱신하는 기준 명단 |
| 집중교육 참석 | 교육관리 스프레드시트 | 손으로 실행해 합치는 입력 |
| 등록 명단·군 현황·수료현황·결산 | 등록 스프레드시트 | 자동화가 갱신 |
| 집중교육 신청(관리용) | 비공개 관리자 스프레드시트 | 전화번호 포함 |
| 집중교육 신청(공개) | 공개 확인 스프레드시트 | 전화번호 없음 |
| 실행 요약 로그 | 각 스프레드시트의 숨김 로그 시트 | 최신순으로 새 줄을 더한다 |
| 처리 커서·보정 상태 | Apps Script 스크립트 속성 | 타임스탬프, 매칭 키, 이전·자동 보정값, 수동 보호 상태 |

보관 기간은 정하지 않고, 자동 삭제도 하지 않는다(NFR-DATA-001).

군 현황판은 14열로 다시 쓴다. 1~2행이 머리글, 3행이 합계, 4행부터 명단이다. 다시 쓰기 전에, 예전 20열 배치가 3행부터 아래에 남긴 내용과 배경색을 먼저 지운다.

수료현황은 2행부터 15열의 기존 내용을 지운 뒤 다시 쓴다. 수료현황의 문자 제외·미확인·비고 값은 기존 전화번호가 정확히 한 줄에 대응할 때만 남긴다.

보정 상태 속성에도 개인정보 값이 들어 있을 수 있다.

## 8. Configuration 사양

| 항목 | 위치 | 기본값 | 설명 |
|---|---|---|---|
| `active` | 교육·등록 설정 객체 | `true` | 거짓이면 예약 실행이 아무 작업도 하지 않는다 |
| `mode` | 교육·등록 설정 객체 | `PRODUCTION` | `TEST`면 테스트 수신자 한 명에게만 보낸다 |
| `testRecipient` | 교육·등록 설정 객체 | 필수 | 안전장치가 고정하는 수신자 한 명 |
| 운영 수신자 목록 | 교육·등록 설정 객체 | 필수 | 프로젝트마다 한 곳에서만 관리 |
| `groups` | 등록 설정 객체 | 9개 군 약칭 | **회기마다 바뀐다.** 이 배열의 순서가 군 현황판 열 순서이고, 열 수는 `군 수 + 5`로 계산한다. 등록·보고 프로젝트에서 군을 정하는 곳은 여기 한 곳뿐이다 (8.1) |
| `groupRecipients` | 등록 설정 객체 | 군별 담당자 | 키 목록이 `groups`와 같아야 한다. 어긋나면 테스트가 실패한다 |
| 스프레드시트·시트 이름 | 각 설정 객체 | 필수 | 운영 코드가 기대는 이름(Runtime contract). 임의로 바꾸지 않는다 |
| 처리 커서 속성 이름 | 교육 설정 객체 | 필수 | 타임스탬프 커서와 예전 방식의 행 번호 커서 |
| 예배 구분 기준 시각 | 교육 설정 객체 | 13:30 | 이전은 4부, 이후는 5부 |
| 조회 기간·고정 시작일 | 문자공지 설정 객체 | 15주 / 고정일 | 문자공지 대상을 찾는 범위 |
| 허용 주차·군·성별 | 웹앱 설정 객체 | 1~4주차 / 군 목록 | 서버에서 확인하는 기준 |
| script ID 매니페스트 | `scripts/apps-script-projects.json` | 필수 | 동기화 대상 네 프로젝트 |
| clasp refresh token | 사용자 홈의 clasp 설정 파일 | 필수 | 저장소에 두지 않는다 |

웹앱·교육·등록의 공통 설정과 문자공지 설정은 `Object.freeze`로 바꿀 수 없게 막는다. 집중교육 신청 설정은 일반 객체다.

> **주의** `Object.freeze`로 막아도 그 안에 든 배열까지 바꿀 수 없게 되지는 않는다.

### 8.1 회기 전환 시 군 구성 변경 절차

군 구성은 **회기(3월 시작)마다 바뀐다.** 새 회기는 이전 회기 스프레드시트를 복사해 시작한다. 이전 회기 스프레드시트와 그 데이터는 그대로 남겨 둔다.

군 이름은 네 Apps Script 프로젝트에 걸쳐 있다. 그래서 **아래 순서대로 전부 고친다.**

> **주의** 한 곳이라도 빠뜨리면 자동화는 멈추지 않고 틀린 결과를 낸다. 사람만 없어진 것처럼 보이기 때문에 알아차리기 어렵다.

#### 코드에서 고치는 자리

| 순서 | 프로젝트 | 위치 | 고치는 것 | 빠뜨리면 |
|---|---|---|---|---|
| 1 | 등록·보고 | `등록새가족-군현황 자동 배치.gs`의 `REGISTRATION_AUTOMATION.groups` | 군 약칭 배열. **배열 순서 = 현황판 열 순서** | 새 군 등록자가 전부 미배정 열로 간다 |
| 2 | 등록·보고 | 같은 파일의 `groupRecipients` | 군 → 담당자 메일. 키를 1과 정확히 같게 | 군별 통계 메일이 가지 않는다 |
| 3 | 등록·보고 | `제목 없음.gs`의 결산 군 행 맵 | 결산 시트의 군별 행 번호 | 결산 집계가 다른 군에 쌓인다 |
| 4 | 교육 관리 | `교육 출석 현황 업데이트.gs`의 유효 군 목록 | 군 첫 글자 허용 목록 | 새 군 출석이 전부 검토로 빠진다 |
| 5 | 출석 웹앱 | `attendance-webapp/Code.gs`의 허용 군 목록 | 서버 확인 목록(`신군` 형태) | 제출이 거부된다 |
| 6 | 출석 웹앱 | `attendance-webapp/Index.html`의 군 `<option>`과 군→팀 매핑 | 화면 선택지 | 선택지에 없어 입력 자체가 안 된다 |
| 7 | 집중교육 신청 | `intensive-training-application/Code.gs`의 `GROUPS`와 군→팀 맵 | Form 생성 기준 | Form을 다시 만들면 이전 회기 군이 나온다 |

1과 2는 등록·보고 프로젝트 안에서 군을 정하는 **유일한** 자리다. 현황판 열 번호와 열 수, 군 표기 정규화는 모두 1에서 계산된다. 그래서 열 번호를 손으로 세지 않는다.

#### 시트에서 하는 일

1. 새 회기용으로 스프레드시트를 복사한다.
2. 각 프로젝트 설정의 스프레드시트 ID(`registrationSpreadsheetId`, `educationSpreadsheetId`, `attendanceSpreadsheetId`)를 복사본 ID로 바꾼다. 이 값을 바꾸지 않으면 새 회기 작업이 이전 회기 시트를 고친다.
3. 복사본의 `등록 새가족` 시트에서 이전 회기 명단을 비운다.
4. `등록 새가족 군 현황`의 3행부터 아래를 비운다. **1~2행 머리글은 손대지 않는다.** 첫 실행이 새 군 구성에 맞춰 다시 쓴다(REQ-REG-002).

#### 확인

```bash
node tests/registration-automation.test.js      # 1과 2가 어긋나면 실패한다
node tests/attendance-webapp.test.js
node tests/intensive-training-application.test.js
```

고친 프로젝트는 각각 배포해야 반영된다(`scripts/apps-script.mjs`). 출석 웹앱은 코드 배포와 별도로 **웹앱 재배포**가 필요하다.

#### 이전 회기 군으로 남은 데이터

없어진 군으로 등록된 줄은 미배정 열로 간다. 결함이 아니고 의도한 동작이다.
이유: 이렇게 하면 이름이 현황판에서 사라지지 않고 담당자에게 메일로 보고된다(REQ-REG-002, REQ-REG-003).

## 9. 오류 처리 정책

- 교육 새 응답 처리의 범위는 저장된 커서로 정한다.
- 시트, 커서, 로그, 메일은 한꺼번에 성공하거나 한꺼번에 취소되는 한 묶음(트랜잭션)이 아니다. 그래서 어느 단계에서 실패했느냐에 따라 다음 실행이 복구하지 못하는 작업이 있을 수 있다. 실패하면 커서와 시트에 반영된 상태를 확인한다.
- Apps Script 트리거 실패 알림은 Apps Script 설정에 맡긴다.
- **부분 성공을 허용한다.** 자동으로 판단할 수 있는 줄만 반영한다. 나머지는 시트를 바꾸지 않고 검토·중복·충돌로 나눠 메일과 숨김 로그에 남긴다. 검토 항목을 짐작으로 자동 수정하지 않는다.
- **멈춰야 하는 조건**은 다음과 같다.
  - 필요한 시트나 스프레드시트를 찾지 못한 경우
  - 잠금을 얻지 못한 경우
  - 메일 수신자 안전장치를 어긴 경우
  - 웹앱 입력 확인에 실패한 경우
  - 웹앱에서 요청한 주차가 기존 기록과 맞지 않는 경우
  - `push` 뒤 다시 확인했을 때 다른 경우
  - 집중교육 설치 확인에 실패한 경우(만든 것을 정리해 본 뒤 멈춘다)
- **아무 일도 하지 않고 정상으로 끝나는 조건**
  - `active`가 거짓
  - 새 응답 0건
  - 처리 기준 커서가 없어 초기화가 필요한 경우
- **기록 위치**
  - 실행 요약: 각 스프레드시트의 숨김 로그 시트
  - 상세: 결과 메일
  - 예외와 진행 로그: Apps Script 실행 로그
- 미리보기 함수는 어떤 실패 경로에서도 시트·속성·메일을 바꾸지 않는다.

## 10. 보안 요구사항

- 인증 수단은 두 가지다.
  - Apps Script 실행 계정의 권한(시트·메일·드라이브)
  - 로컬 동기화 도구가 다시 쓰는 clasp refresh token. 토큰 파일은 사용자 홈 폴더에만 두고 저장소에서 뺀다.
- 저장소에 들어가면 안 되는 값: 개인 명단, 실행 결과, 인증 토큰, 개인 절대경로.
- 스프레드시트·스크립트 식별자는 운영 코드에 필요한 만큼만 두고, 이 사양 문서에는 쓰지 않는다.
- 출석 웹앱은 로그인 없이 누구나 열 수 있다. 다만 실행 권한은 배포한 사람의 계정이고, 조회할 때 이름과 전화번호를 모두 요구한다.
- 다른 웹페이지 안에 넣어 보여 주는 것(외부 iframe 임베드)은 허용하지 않는다.
- 사용자 입력을 HTML에 넣기 전에 이스케이프한다.
- 집중교육 관리자 파일은 비공개이고, 공개 확인 시트만 링크 뷰어 권한이다(NFR-SEC-001).
- 운영 수신자, 시트 식별자·이름·열 위치, 트리거 함수 이름, `active`, `mode`는 근거 없이 바꾸지 않는다.

## 11. 테스트 사양

ID 규칙: `TEST-<CATEGORY>-NNN`.

아래 테스트는 모두 `npm test`(`scripts/run-tests.mjs`)로 한 번에 돌린다. 모두 Apps Script API를 부르지 않는 **정적·단위 검사**다. 시트 권한, 트리거, 메일 도착은 확인하지 않는다.

### TEST-ATTEND-001

#### 검증 대상
REQ-ATTEND-001

#### 선행 조건
Node.js 18 이상. 따로 설치할 패키지는 없다.

#### 절차
1. `tests/attendance-webapp.test.js`를 실행한다.
2. 웹앱 서버 소스를 격리된 실행 환경(VM)에 올려, 이름·전화번호 정규화, 전화번호 유효성, 주차 읽기 함수를 부른다.
3. 허용되지 않은 주차(5주차)로 제출 확인 함수를 부른다.
4. 소스에 스크립트 잠금 사용, 화면 쪽 이스케이프, 실패 처리기가 있는지 확인한다.

#### Expected Result
- 정규화·유효성 결과가 기대값과 같다.
- 5주차 제출이 "허용되지 않은 교육 주차" 예외로 거부된다.
- 잠금·이스케이프·실패 처리기 패턴이 모두 발견된다.

### TEST-ATTEND-002

#### 검증 대상
REQ-ATTEND-002

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/attendance-webapp.test.js`에서 가짜 시트 객체로 끼워 넣기 함수를 부르고, 호출 순서를 기록한다. 서버 소스에 시트 끝에 줄을 붙이는 호출이 남아 있지 않은지도 확인한다.

#### Expected Result
다음 순서가 정확히 관찰된다.

1. 2행 앞에 줄을 끼워 넣는다.
2. 2행에 값을 쓴다.
3. 타임스탬프 열에 날짜 형식을 적용한다.

`appendRow` 호출은 발견되지 않는다.

### TEST-MAIL-001

#### 검증 대상
REQ-MAIL-001

#### 선행 조건
Node.js 18 이상.

#### 절차
1. `tests/education-automation.test.js`와 `tests/registration-automation.test.js`를 실행한다.
2. 메일 API를 가짜로 바꾼 VM에서 공용 발송 관문을 세 가지 방식으로 부른다.
   - 일반 발송
   - 테스트 강제 발송
   - 운영 수신자 목록을 직접 넘긴 테스트 강제 발송
3. `run*Test` 진입점 소스가 테스트 강제 옵션을 켜는지, 정기 발송 경로는 켜지 않는지 확인한다.

#### Expected Result
- 일반 발송은 운영 수신자 5명에게 가고, 제목에 `[TEST]`가 없다.
- 테스트 강제 발송은 수신자가 테스트 주소 한 명이고, 제목이 `[TEST]`로 시작한다.
- 운영 목록을 직접 넘겨도 결과는 같다.
- `run*Test` 진입점에서 강제 옵션이 발견되고, 토요일 정기 발송은 운영 5명을 유지한다.

### TEST-MAIL-002

#### 검증 대상
REQ-MAIL-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/education-notification.test.js`를 실행한다. 교육 설정과 문자공지 소스를 함께 올려 발송 관문을 한 번 부른다. 문자공지 소스가 자기만의 운영 수신자 목록을 정의하는지도 검사한다.

#### Expected Result
- 수신자가 설정 객체의 운영 목록과 순서까지 같은 5명이고, 중복이 없다.
- 문자공지 소스에는 따로 정의한 운영 수신자 목록이 없다.

### TEST-LOG-001

#### 검증 대상
REQ-LOG-001

#### 선행 조건
Node.js 18 이상.

#### 절차
1. `tests/education-automation.test.js`와 `tests/registration-automation.test.js`를 실행한다.
2. 가짜 스프레드시트로 로그 시트를 가져오는 함수를 두 상황에서 부른다.
   - 로그 시트가 없을 때
   - 로그 시트가 이미 있고 보이는 상태일 때
3. 로그 기록 함수가 빈 함수(stub)로 되돌아가지 않았는지, 실행 경로에서 불리는지를 소스에서 확인한다.

#### Expected Result
- 새로 만든 로그 시트와 기존에 보이던 로그 시트가 모두 숨김 상태가 된다.
- 활성 시트는 원래 운영 시트로 남는다.
- 로그 기록 함수는 stub이 아니고, 각 실행 경로에서 불린다.

### TEST-REG-001

#### 검증 대상
REQ-REG-002

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 연도 짐작 함수를 여러 기준일로 부른다.

- 9월에 실행할 때의 12월과 3월
- 12월에 실행할 때의 12월
- 해가 바뀐 이듬해 6월에 실행할 때의 12월과 3월

연도를 고정한 표현이 소스에 남아 있는지도 확인한다.

#### Expected Result
- 회기 시작 전 12월은 전년도, 3월은 그해로 읽는다.
- 12월에 실행할 때의 12월은 그해다.
- 해가 바뀌어도 같은 규칙이 적용된다.
- 연도를 코드에 박아 둔 표현은 발견되지 않는다.

### TEST-REG-002

#### 검증 대상
REQ-REG-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 자동 보정 항목을 정하는 함수(`registrationCorrectionFields_`)를 등록 줄과 교육 출석 매칭 결과로 부른다.

돌려받은 항목의 키 목록을 확인한다. 이름이 다른 매칭에서 검토 사유가 만들어지는 소스 경로도 확인한다.

#### Expected Result
- 자동 보정 항목은 군·팀·전화번호 세 개뿐이고, 이름은 들어 있지 않다.
- 이름이 다르면 등록 줄을 바꾸지 않고 검토 내역으로 보고하는 코드가 발견된다.

### TEST-REG-003

#### 검증 대상
REQ-REG-002

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 군 현황판 열을 정하는 함수(`resolveDashboardGroupColumn_`)를 여러 등록 줄 형태로 부른다.

- 4부 "신", 5부 "신", 4부 "임"
- 예배 구분 없는 "석"
- 오타 "신군"
- 빈 군 + 소개자 "스스로"(4부·5부)
- 예배 구분 없는 "스스로"
- 빈 군 + 빈 소개자

머리글을 만드는 함수가 돌려주는 값과 열 구성 상수도 함께 확인한다.

#### Expected Result
- 같은 군은 예배 구분과 관계없이 같은 열 번호를 받는다.
- "스스로"만 4부·5부가 서로 다른 열을 받는다.
- 군을 확정할 수 없는 값과 예배 구분 없는 "스스로"는 `assigned: false`와 함께 `미배정` 열 번호를 받는다.
- 어떤 입력에서도 열 번호가 없는 결과는 나오지 않는다.
- 머리글 2행에는 9개 군과 `4부`·`5부`가 열 구성 상수와 같은 자리에 놓인다.

### TEST-REG-004

#### 검증 대상
REQ-REG-003

#### 선행 조건
Node.js 18 이상.

#### 절차
1. `tests/registration-automation.test.js`에서 시험용으로 지어낸 등록 명단으로 `updateNewFamilyStatus_`를 실행한다.
2. 돌려받은 명단의 구성과 각 항목의 필드를 확인한다.
3. 그 결과로 텍스트·HTML 메일 본문을 만드는 함수를 불러, 명단의 모든 이름이 두 본문에 들어가는지 확인한다.
4. 수신자 설정이 한 명으로 고정돼 있는지 소스에서 확인한다.

#### Expected Result
- 스스로 열에 놓인 사람과 미배정 사유가 있는 사람이 모두 명단에 들어가고, `스스로`·`미배정` 구분이 맞다.
- 각 항목에 등록 시트 행 번호와 등록일이 있다.
- 명단의 모든 이름이 텍스트 메일과 HTML 메일 본문에서 발견된다.
- 세기만 하고 본문에 넣지 않은 상태는 실패로 잡힌다.

### TEST-REG-005

#### 검증 대상
REQ-REG-002

#### 선행 조건
Node.js 18 이상.

#### 절차
1. `tests/registration-automation.test.js`에서 **1행이 필터 머리글인 가짜 시트**로 `updateNewFamilyStatus_`를 실행한다. 이 가짜 시트의 `merge()`는 필터 머리글을 포함한 세로 병합을 Google Sheets와 같은 문구로 거부한다.
2. 다음을 본다.
   - 실행이 예외 없이 끝나는지
   - 모든 등록자가 기록되는지
   - 세로 병합을 시도한 호출이 있는지
   - 병합 없이도 2행에 모든 열 제목이 있는지
3. 머리글 쓰기가 실패하도록 만든 시트로 다시 실행한다. 예외가 올라오면서도 명단은 이미 기록돼 있는지 확인한다.

#### Expected Result
- 필터가 걸린 시트에서도 예외 없이 끝나고, 등록자 전원이 기록된다.
- 이전 배치의 잔여 내용과 머리글은 남지 않는다.
- 세로 병합 호출은 0건이다.
- `날짜`·`미배정`·`합계`는 2행에만 있고, 1행에는 묶음 이름 두 개 말고는 아무 값도 없다.
- 머리글 쓰기가 실패한 경우에는 예외가 올라오지만 명단은 온전히 남아 있다.

이 테스트는 고치기 전 구현에서는 실패해야 한다(운영 사고 재현).

### TEST-REG-006

#### 검증 대상
REQ-REG-004

#### 선행 조건
Node.js 18 이상.

#### 절차
1. `tests/registration-automation.test.js`에서 시험용으로 지어낸 등록 명단으로 `updateNewFamilyStatus_`를 실행한다.
2. 3행 합계 행의 각 열 값을 **명단 영역을 직접 세어 얻은 값**과 비교한다.
3. 합계 열의 총합이 전체 인원과 같은지, 주별 합계를 따로 더한 값과도 같은지 본다.
4. 등록일을 읽을 수 없는 줄을 하나 더해 다시 실행한다. 대조가 불일치를 잡는지, 빠진 사람의 이름과 행 번호가 결과 문장에 있는지, 검토 내역에 보고되는지 확인한다.

#### Expected Result
- 합계 행의 모든 값이 실제 배치와 같다.
- 합계 열 총합과 주별 합계의 합이 전체 인원과 같다.
- 정상 데이터에서는 대조가 통과하고, 결과 문장에 근거 인원 수가 들어 있다.
- 배치하지 못한 사람이 생기면 대조가 실패로 바뀐다. 그 사람의 이름과 등록 행 번호가 문장에 나오고, 검토 내역에도 보고된다.
- 합계 행을 명단으로 잘못 읽지 않는다.

### TEST-SETTLE-001

#### 검증 대상
REQ-SETTLE-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 결산 날짜 정규화 함수와 행축 판정 함수를 여러 입력으로 부른다. 입력은 `3/7`, `3/8`, `5/12`, `12/25`, `2026-05-12` 형식과 빈 값이다.

목표값 열을 남겨 두는지, 합계 행과 검토 내역을 쓰는지도 소스에서 확인한다.

#### Expected Result
- `3/8` 이후만 행축 후보가 되고, 12월~2월은 일반으로 남는다.
- 연도가 붙은 날짜도 `M/d`로 정규화된다.
- 목표값 열을 덮어쓰는 코드는 발견되지 않는다.

### TEST-PERF-001

#### 검증 대상
NFR-PERF-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/registration-automation.test.js`에서 등록 유지관리 소스가 스크립트 속성을 어떻게 읽고 쓰는지 검사한다.

#### Expected Result
줄마다 속성을 하나씩 읽는 호출은 없다. 전체를 한 번에 읽는 호출과 한 번에 저장하는 호출이 발견된다.

### TEST-INTENSIVE-001

#### 검증 대상
REQ-INTENSIVE-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/intensive-training-application.test.js`를 실행한다. 다음을 확인한다.

- 군 수와 군별 팀 수
- 전화번호 정규화와 형식 확인
- 군 표기 변환·복원
- 같은 머리글이 되풀이되는 Section에서 응답값 고르기
- 트리거가 관리자 Spreadsheet 제출 이벤트로 설치되는지(소스에서 확인)

#### Expected Result
- 군은 9개이고, 군별 팀 수가 기대값과 같다.
- 숫자만 넣은 번호가 하이픈 형식으로 정규화되고, 10자리는 거부된다.
- 군 표기 변환·복원과 Section별 응답값 고르기가 기대값과 같다.
- Form 이벤트 트리거는 없고, Spreadsheet 제출 이벤트 트리거만 발견된다.

### TEST-SEC-001

#### 검증 대상
NFR-SEC-001

#### 선행 조건
Node.js 18 이상.

#### 절차
`tests/intensive-training-application.test.js`에서 관리자 파일 비공개 설정과 공개 시트 링크 뷰어 설정이 소스에 있는지 확인한다.

> **주의** 머리글 검사는 테스트 안에 정의한 기대 머리글 목록을 대상으로 한다. 그래서 실제 생성된 시트의 머리글 검증을 대신하지 못한다.

#### Expected Result
- 관리자 파일 비공개 설정과 공개 시트 링크 뷰어 설정이 모두 발견된다.
- 기대 머리글 목록에는 전화번호·연락처·이메일·계정·응답 ID에 해당하는 열이 없다.

### TEST-EDU-001

#### 검증 대상
REQ-EDU-001

#### 선행 조건
운영 Sheets 읽기 권한이 필요하다. 이 테스트는 자동화하지 않는다.
이유: 커서는 스크립트 속성에, 응답은 운영 시트에 있어서 두 바깥 상태가 모두 필요하다.

아래 절차는 **시트·속성·메일을 바꾸지 않는** 미리보기 함수만 쓴다.

#### 절차
1. `previewPendingAttendance()`를 실행해 예정 건수와 기준 시각을 확인한다.
2. 응답 시트에서 그 기준 시각 이후 줄 수를 직접 세어 예정 건수와 맞는지 본다.
3. 한 번 더 실행해 같은 수가 나오는지 확인한다. 미리보기가 커서를 옮기지 않는지 보는 것이다.
4. 속성에 `EDUCATION_LAST_RESPONSE_ROW`(행 번호 커서)가 남아 있으면 `migrateEducationCursorAndProcessPending()`으로 환산되는지 확인한다.

#### Expected Result
- 커서보다 늦은 응답만, 타임스탬프가 이른 순서로 예정된다.
- 미리보기는 커서를 바꾸지 않아서 여러 번 실행해도 결과가 같다.
- 커서가 없고 환산도 안 되면 아무것도 처리하지 않고 초기화가 필요하다고 보고한다.

### TEST-EDU-002

#### 검증 대상
REQ-EDU-002

#### 선행 조건
TEST-EDU-001과 같다. 매칭 판정은 운영 교육 시트의 전화번호 분포에 달려 있다. 그래서 시험용으로 지어낸 데이터(합성 fixture)로는 사양을 확인할 수 없다.

#### 절차
1. `previewPendingAttendance()`의 검토 대상 분류를 연다.
2. 다음 경우를 하나씩 골라 분류를 확인한다.
   - 같은 전화번호가 여러 줄인 응답
   - 2주차인데 전화번호로 못 찾은 응답
   - 일요일이 아닌 때 제출된 1주차 응답
   - 이미 값이 있는 주차 칸

#### Expected Result
네 경우 모두 자동 반영되지 않고 검토 대상이나 중복으로 분류된다. 자동 반영은 정규화한 전화번호가 정확히 한 줄과 같을 때뿐이다.

### TEST-EDU-003

#### 검증 대상
REQ-EDU-003

#### 선행 조건
Node.js 18 이상. 외부 접근 없음.

#### 절차
`tests/education-automation.test.js`를 실행한다(`npm test`). 집중교육 반영 부분에서 다음을 확인한다.

- 숫자 `0`과 영문 `o`·`O`는 참석으로 읽고 빈칸과 `X`는 제외하는지
- 분기 번호 `3-1`이 날짜로 저장돼도 4주차에 `3분기 집중교육`을 적는지
- 이전 실행의 날짜 문자열 표기는 고치고, 다른 4주차 값은 그대로 두는지
- 시트를 열면 `집중교육` 메뉴에 `참석 명단 반영` 한 항목이 생기는지
- 메뉴 실행이 실제 반영 함수를 호출하고 추가·갱신·검토 필요 건수를 알림창에 보여 주는지
- 실제 반영 함수가 실패하면 실패 원인을 알림창에 보여 주고 예외를 유지하는지
- 미리보기 함수와 실제 반영 함수의 이름이 나뉘어 있는지
- `run*Test` 이름이 실제 반영에 쓰이지 않는지

#### Expected Result
세 참석 표시는 모두 반영 대상이다. 날짜로 저장된 번호도 보이는 분기로 기록한다. 시트 메뉴에서 한 번 실행하면 실제 반영 함수가 한 번 호출되고 결과 건수가 표시된다. `previewIntensiveTraining()`은 `dryRun: true`로 동작한다. 실제 반영 함수에는 미리보기로 오해할 만한 이름(`runIntensiveTrainingTest`)이 없다.

### TEST-NOTI-001

#### 검증 대상
REQ-NOTI-001

#### 선행 조건
Node.js 18 이상. 메일은 가짜 발송기가 가로채므로 밖으로 나가지 않는다.

#### 절차
`tests/education-notification.test.js`를 실행한다(`npm test`).

#### Expected Result
- 문자 명단 메일은 운영 수신자 5명에게 정확히 한 번 가고, 중복이 없다.
- 제목에 `[TEST]`가 붙지 않는다.
- 문자 명단 스크립트는 자기만의 수신자 목록을 가지고 있지 않다. 수신자는 `EDUCATION_AUTOMATION.productionRecipients` 한 곳에서만 관리한다(두 곳에서 관리하지 않기 위해서다).

### TEST-REPORT-001

#### 검증 대상
REQ-REPORT-001

#### 선행 조건
등록·교육·수료현황 세 시트를 읽을 권한이 필요하다. 이 테스트는 자동화하지 않는다.
이유: 세 시트를 합친 결과가 검증 대상이다.

아래 절차는 시트를 바꾸지 않는 미리보기만 쓴다.

#### 절차
1. `previewEducationDetailedReport()`를 실행해 합친 결과와 불일치 보고를 확인한다.
2. 등록 시트의 군·팀·전화번호가 그대로인지(자동으로 덮어쓰지 않았는지) 확인한다.
3. 테스트 실행이 메일을 한 통만 보내는지 확인한다.

#### Expected Result
- 전화번호로 합친 결과가 나오고, 불일치는 보고만 된다.
- 미리보기는 시트를 바꾸지 않는다.
- 테스트 실행은 메일이 한 통이다.

### TEST-OPS-001

#### 검증 대상
NFR-OPS-001

#### 선행 조건
Node.js 18 이상. 외부 접근 없음.

#### 절차
`tests/education-automation.test.js`에서 금요일 트리거 삭제 도우미 부분을 실행한다(`npm test`).

#### Expected Result
`removeFridayEducationTrigger()`는 금요일 `main` CLOCK 트리거(시각으로 예약한 실행)만 지우고, 다른 트리거는 건드리지 않는다.

### TEST-DATA-001

#### 검증 대상
NFR-DATA-001

#### 선행 조건
Node.js 18 이상. 외부 접근 없음.

#### 절차
`tests/data-retention-and-sync.test.js`를 실행한다(`npm test`).

#### Expected Result
여섯 개 `.gs` 파일 어디에도 다음 호출이 없다: 시트·줄 삭제, 속성 한꺼번에 삭제, 내용 비우기.

보존 기간 정책 자체는 코드로 검증하지 않는다. 다만 **자동 삭제 코드를 두지 않는다**는 금지는 검증한다.
이유: 검증하지 않으면 누군가 정리 코드를 넣어도 드러나지 않는다.

### TEST-SYNC-001

#### 검증 대상
NFR-SYNC-001

#### 선행 조건
Node.js 18 이상. 매니페스트 검사는 바깥에 접속하지 않는다. `diff`/`push` 확인은 clasp 자격 증명이 필요해서 손으로 한다.

#### 절차
1. `tests/data-retention-and-sync.test.js`를 실행한다(`npm test`).
2. (수동) `npm run diff`로 운영과 로컬이 같은지, 다른지, 한쪽에만 있는지 확인한다.

#### Expected Result
- 매니페스트에 네 프로젝트가 있다. 각 `scriptId` 형식과 `dir` 폴더가 있는지 확인되고, 중복이 없다.
- 웹앱 프로젝트는 하나다.
- clasp 인증 파일은 git이 추적하지 않는다.
- `push` 경로에 반영한 뒤 다시 읽어 같은지 확인하는 단계가 남아 있다.
- 손으로 돌린 `diff`는 의도한 차이만 보여야 한다.

## 12. 요구사항 추적성

| Requirement | Implementation | Test | Status |
|---|---|---|---|
| REQ-ATTEND-001 | `attendance-webapp/Code.gs` | TEST-ATTEND-001 | implemented |
| REQ-ATTEND-002 | `attendance-webapp/Code.gs` | TEST-ATTEND-002 | implemented |
| REQ-EDU-001 | `education-project/교육 출석 현황 업데이트.gs` | TEST-EDU-001 (수동) | implemented |
| REQ-EDU-002 | `education-project/교육 출석 현황 업데이트.gs` | TEST-EDU-002 (수동) | implemented |
| REQ-EDU-003 | `education-project/집중교육 출석 현황 업데이트.gs` | TEST-EDU-003 | implemented |
| REQ-NOTI-001 | `education-project/문자 명단 리스트.gs` | TEST-NOTI-001 | implemented |
| REQ-REG-001 | `registration-project/등록새가족-군현황 자동 배치.gs` | TEST-REG-002 | implemented |
| REQ-REG-002 | `registration-project/등록새가족-군현황 자동 배치.gs` | TEST-REG-001, TEST-REG-003, TEST-REG-005 | implemented |
| REQ-REG-003 | `registration-project/등록새가족-군현황 자동 배치.gs` | TEST-REG-004 | implemented |
| REQ-REG-004 | `registration-project/등록새가족-군현황 자동 배치.gs` | TEST-REG-006 | implemented |
| REQ-REPORT-001 | `registration-project/등록 새가족 새가족교육 수료현황 자동화.gs` | TEST-REPORT-001 (수동) | implemented |
| REQ-SETTLE-001 | `registration-project/제목 없음.gs` | TEST-SETTLE-001 | implemented |
| REQ-INTENSIVE-001 | `intensive-training-application/Code.gs` | TEST-INTENSIVE-001 | implemented |
| REQ-MAIL-001 | `education-project/교육 출석 현황 업데이트.gs`, `registration-project/등록새가족-군현황 자동 배치.gs` | TEST-MAIL-001, TEST-MAIL-002 | implemented |
| REQ-LOG-001 | `education-project/교육 출석 현황 업데이트.gs`, `registration-project/등록새가족-군현황 자동 배치.gs` | TEST-LOG-001 | implemented |
| NFR-SEC-001 | `intensive-training-application/Code.gs`, `.gitignore` | TEST-SEC-001 | implemented |
| NFR-OPS-001 | `education-project/교육 출석 현황 업데이트.gs`, `registration-project/등록새가족-군현황 자동 배치.gs` | TEST-OPS-001 | implemented |
| NFR-DATA-001 | `education-project/교육 출석 현황 업데이트.gs`, `registration-project/등록새가족-군현황 자동 배치.gs` (자동 삭제 코드를 두지 않는다는 금지) | TEST-DATA-001 | implemented |
| NFR-PERF-001 | `registration-project/등록새가족-군현황 자동 배치.gs` | TEST-PERF-001 | implemented |
| NFR-SYNC-001 | `scripts/apps-script.mjs`, `scripts/apps-script-projects.json` | TEST-SYNC-001 | implemented |


Status 값: `draft` (사양만 있음) / `implemented` / `verified` (실제 실행까지 확인) /
`deprecated`.

테스트 연결은 그 요구사항을 일부 확인한 근거일 뿐이다. 기능 전체를 운영에서 확인했다는 뜻은 아니다.

이번 문서 도입 때는 Sheets 권한, 트리거, 메일 도착을 확인하지 않았다. 그래서 상태를 모두 `implemented`로 둔다. 운영 검증은 따로 승인받은 범위에서 한다.

## 13. 미확정 사항

- **REQ-SETTLE-001 산출 기준의 소유자 확인.** 2026-09-19에 코드를 근거로 사양을 확정했다. 확정한 기준은 군 9개 행 매핑, `3/8` 행축 기준, 유입 경로 `관계`·`설문지`, 일반·행축 목표 열이다. 운영 기준이 다르면 코드보다 이 사양을 먼저 고친다.
- **상반기 방문자 동기화를 다시 켜는 시점.** 하반기 시트를 준비한 뒤 다시 켜기로 했고, 시점은 정하지 않았다. 지금은 일부러 꺼 두었다.

## 14. 향후 개선 후보

- 상반기 방문 새가족 동기화 다시 켜기. 지금 이 함수는 일부러 꺼 두었고, 하반기 시트를 준비한 뒤 다시 켜기로 했다.
- 정착률 월간 메일 자동화. 분석은 끝났지만 Apps Script 밖의 실행 환경이 필요해 범위에서 뺐다(`docs/settlement-monthly-email-analysis.md`).
- 교육 새 응답 반영(REQ-EDU-001)과 매칭·검토 분류(REQ-EDU-002)의 단위 테스트. 이 프로젝트에서 가장 복잡한 판단 로직인데 로컬 검증이 없다.
- 문자공지 대상자 선정 기준(REQ-NOTI-001)의 단위 테스트.
